# 智能项目管理系统操作手册

## 1. 登录系统

访问 `http://localhost`，输入用户名和密码登录。默认密码均为 `123456`。

| 身份 | 用户名 | 主要功能 |
|---|---|---|
| 管理员 | `admin` | 全部功能、立项审核、项目启动、系统配置、用户管理 |
| 商务 | `business01` | 立项草稿、合同解析、差异确认、提交审核 |
| 财务 | `finance01` | 开票登记、回款登记、台账、结项审批、报表导出 |
| 项目经理 | `pm01` | 查看项目、查看财务明细、提交结项 |

管理员是超级角色，除系统管理外，也可以处理商务、财务和项目经理的业务任务。

## 2. 商务立项操作

1. 使用商务账号登录。
2. 进入“立项管理”。
3. 上传 Word 或 `.doc` 合同。
4. 系统自动解析项目名称、合同金额、甲方、乙方、丙方、签订日期、合同编号等字段。
5. 如果合同没有明确字段标志，可在“系统管理 - LLM 配置”启用大模型兜底解析。
6. 核对表单信息，必要时手工补充或修正。
7. 点击保存草稿，刷新页面后草稿仍会保留。
8. 如需重新上传合同，可先取消已上传文件，再选择新文件解析。
9. 上传盖章 PDF/图片合同，系统 OCR 后生成差异。
10. 在差异表中选择人工采用值并确认。
11. 点击提交审核，项目进入待审核。

注意：

- 合同编号允许为空。
- 项目编号、招标编号、采购编号不会作为合同编号保存。
- 合同金额优先取“本合同总金额/合同总金额/合同金额”，不会取分期付款金额。

## 3. 管理员审核与系统管理

### 3.1 立项审核

1. 使用管理员账号登录。
2. 进入“立项管理”。
3. 在项目列表中查看待审核项目。
4. 点击查看详情，核对商务提交的完整立项信息、合同附件、差异确认结果和审批记录。
5. 填写审批备注。
6. 选择通过或驳回。
7. 审核通过后项目状态为已立项。
8. 对已立项项目点击确认开始，项目状态变为进行中。

驳回后项目仍为草稿，但页面会显示已驳回草稿，商务可以修改后重新提交。

### 3.2 用户管理

进入“系统管理 - 用户管理”：

- 可按关键字、角色、状态筛选用户。
- 可新增、编辑、启用、禁用、删除用户。
- 不能禁用或删除当前登录管理员账号。
- 用户只允许绑定一个角色。

### 3.3 LLM 配置

进入“系统管理 - LLM 配置”：

1. 开启或关闭 LLM 兜底解析。
2. 新增一个或多个模型配置。
3. 填写 API Base URL、模型名称和 API Key。
4. 选择当前启用模型。
5. 点击测试连接确认配置可用。
6. 保存后立即生效，无需重启服务。

API Key 不会在页面或日志中明文回显。

## 4. 财务开票回款操作

进入“开票回款”页面，页面内部包含：开票登记、回款登记、发票记录、回款记录、台账。

### 4.1 开票登记

1. 在项目选择框中输入项目编号、合同编号、项目名称或甲方关键字。
2. 系统实时返回匹配项目，也允许手工输入后选择。
3. 上传发票 PDF 或图片。
4. 系统识别发票号码、开票日期、购方、销方、不含税金额、税率、税额、价税合计。
5. 核对字段后保存。
6. 保存成功后发票记录和台账自动刷新。

规则：

- 发票号码全局唯一。
- 开票额度按不含税金额累计校验。
- 已结项项目不可开票。
- 如未保存前发现文件错误，可取消文件并重新上传识别。

### 4.2 回款登记

1. 切换到“回款登记”。
2. 选择或搜索项目。
3. 上传回款凭证 PDF 或图片。
4. 系统优先识别“本次回款金额/本次收款金额”等字段。
5. 核对回款金额、回款日期、付款方、收款方和备注。
6. 保存回款。

规则：

- 回款不再强制关联具体发票。
- 项目累计回款不得超过项目累计开票价税合计。
- 保存后回款记录和台账自动刷新。
- 已保存回款可删除后重新登记。

### 4.3 台账查看

台账展示：

- 合同金额
- 累计开票价税合计
- 累计开票不含税金额
- 累计回款
- 未回款金额
- 开票进度
- 回款进度
- 发票明细
- 回款明细
- 异常提示

## 5. 项目经理结项操作

1. 使用项目经理账号登录。
2. 进入“结项管理”。
3. 搜索并选择已立项或进行中的项目。
4. 查看立项内容、合同差异和财务明细。
5. 填写结项时间、结项说明。
6. 上传验收报告或附件。
7. 若项目回款未完成，系统提示未回款金额；仍允许提交结项审批。
8. 提交后等待财务审批。

财务审批通过后项目状态变为已结项，已结项项目只读且不可继续开票。

## 6. 查询报表与导出

进入“查询报表”：

- 按年查询：展示 1-12 月开票、回款趋势。
- 按月查询：展示当月每日趋势、当月发票记录和当月回款记录。
- 关键字支持项目编号、合同编号、项目名称、甲方/客户。
- 可勾选项目、发票、回款数据生成 Excel。
- 导出任务只存在运行时队列，不写入数据库。
- 导出完成后点击下载文件，通过浏览器下载 `.xlsx`。

## 7. 部署与维护

常用命令：

```powershell
docker compose up -d --build
docker compose up -d --build backend frontend
docker compose ps
docker compose logs -f backend
```

健康检查：

- `http://localhost`
- `http://localhost:8000/health`
- `http://localhost:8001/health`

## 8. 注意事项

- 浏览器显示旧界面时，先执行 `Ctrl + F5` 强制刷新。
- 修改前端或后端代码后，需要重新构建 Docker 镜像。
- 不实现软删除，删除操作按当前业务规则硬删除或受外键约束限制。
- 生产环境应修改 `.env` 中的数据库密码和 `SECRET_KEY`。
