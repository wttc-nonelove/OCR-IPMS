# 智能项目管理系统操作手册

## 1. 访问地址

Docker 部署成功后访问：

- 前端系统：http://localhost
- 后端健康检查：http://localhost:8000/health
- 后端接口文档：http://localhost:8000/docs
- PaddleOCR 占位服务：http://localhost:8001/health

## 2. 默认账号

所有默认账号密码均为 `123456`。

| 身份 | 用户名 | 可用功能 |
|------|--------|----------|
| 管理员 | admin | 立项审核、项目开始确认、结项撤回、系统管理、报表 |
| 商务 | business01 | 立项登记、合同上传、合同差异确认、提交立项审核 |
| 财务 | finance01 | 开票登记、回款登记、结项审核、报表导出 |
| 项目经理 | pm01 | 查看项目、提交结项申请 |

## 3. 推荐演示流程

### 3.1 商务创建并提交立项

1. 使用 `business01 / 123456` 登录。
2. 进入“立项管理”。
3. 填写项目名称、客户名称、合同金额。
4. 可选择上传 Word 合同。
5. 点击“创建草稿”。
6. 在项目列表中点击“提交审核”。

### 3.2 管理员审核并开始项目

1. 退出后使用 `admin / 123456` 登录。
2. 进入“立项管理”。
3. 对待审核项目点击“审核通过”。
4. 对已立项项目点击“开始项目”。
5. 项目状态变为 `active / 进行中`。

### 3.3 财务登记开票和回款

1. 使用 `finance01 / 123456` 登录。
2. 进入“开票回款”。
3. 填写项目 ID、发票号码、金额、开票日期。
4. 点击“保存开票”。
5. 填写项目 ID、发票 ID、回款金额、回款日期。
6. 点击“保存回款”。

### 3.4 项目经理提交结项

1. 使用 `pm01 / 123456` 登录。
2. 进入“结项管理”。
3. 填写项目 ID、结项时间、结项说明。
4. 点击“提交结项”。

### 3.5 财务审批结项

1. 使用 `finance01 / 123456` 登录。
2. 当前 MVP 已实现审批任务接口，前端结项审批按钮后续可继续细化。
3. 可在后端接口文档 `/docs` 中调用 `/api/v1/approval/task/list` 和 `/api/v1/approval/process` 完成审批。
4. 财务审批通过后项目变为 `closed / 已结项`，不可再开票。

## 4. Docker 常用命令

在项目根目录 `F:\课设\OCR-NLP` 执行：

```powershell
docker compose up -d --build
```

查看服务状态：

```powershell
docker compose ps
```

查看日志：

```powershell
docker compose logs -f backend
docker compose logs -f frontend
docker compose logs -f db
```

停止服务：

```powershell
docker compose down
```

清理数据库卷：

```powershell
docker compose down -v
```

## 5. 当前实现边界

- OCR/NLP 当前是本地占位实现，后续可替换为真实 PaddleOCR + 百度 OCR。
- 导出任务不落库，只保存在运行时任务队列中。
- 不实现站内信、邮件、短信、自动催办、自动转交。
- 不实现软删除。
- 数据库透明加密依赖部署环境提供，业务代码不做字段级金额加密。

## 6. 故障排查

### 6.1 前端打不开

检查容器：

```powershell
docker compose ps
docker compose logs frontend
```

确认 80 端口未被其他程序占用。

### 6.2 后端健康检查失败

检查后端和数据库日志：

```powershell
docker compose logs backend
docker compose logs db
```

后端依赖 MySQL 健康检查，首次启动可能需要等待 1 分钟左右。

### 6.3 登录失败

确认后端已完成初始化：

```powershell
curl http://localhost:8000/health
```

默认账号只会在数据库为空时初始化。如果修改过数据库，可使用：

```powershell
docker compose down -v
docker compose up -d --build
```

重建数据库。
