# 智能项目管理系统数据库设计说明书（最终版）

## 1. 数据库环境

- 数据库：MySQL 8.0
- 字符集：utf8mb4
- 排序规则：utf8mb4_unicode_ci
- 存储引擎：InnoDB
- 金额字段：`DECIMAL`
- 加密策略：采用数据库或磁盘透明加密能力，不做字段级金额加密。
- 删除策略：不实现软删除，按业务规则和外键约束处理。

## 2. 表结构总览

| 表名 | 说明 |
|---|---|
| sys_user | 用户表 |
| sys_role | 角色表 |
| sys_dict | 数据字典表 |
| sys_log | 审计日志表 |
| sys_config | 系统配置表，保存 LLM 配置等 |
| project_info | 项目信息表 |
| project_contract | 合同文件表 |
| project_contract_diff | 合同差异确认表 |
| project_cost | 后期支出表 |
| project_invoice | 发票信息表 |
| project_payment | 回款信息表 |
| project_close | 结项信息表 |
| approval_template | 审批模板表 |
| approval_node | 审批节点表 |
| approval_approver | 审批人配置表 |
| approval_instance | 审批实例表 |
| approval_task | 审批任务表 |
| approval_record | 审批记录表 |
| ocr_recognition_log | OCR识别日志表 |

导出任务不落库，不设计导出历史表；导出状态由运行时任务队列维护。

## 3. 核心表设计

### 3.1 sys_user

| 字段 | 类型 | 说明 |
|---|---|---|
| id | INT PK | 用户ID |
| username | VARCHAR(50) UNIQUE | 用户名 |
| password | VARCHAR(255) | BCrypt密码哈希 |
| name | VARCHAR(50) | 姓名 |
| phone | VARCHAR(20) | 手机号 |
| email | VARCHAR(100) | 邮箱 |
| role | VARCHAR(20) | admin/business/finance/pm，单用户单角色 |
| dept | VARCHAR(50) | 部门 |
| status | INT | 1启用，0禁用 |
| create_time/update_time | DATETIME | 创建和更新时间 |

### 3.2 sys_config

| 字段 | 类型 | 说明 |
|---|---|---|
| id | INT PK | 配置ID |
| config_key | VARCHAR(100) UNIQUE | 配置键 |
| config_value | TEXT | 配置值 |
| config_type | VARCHAR(30) | string/bool/json/secret |
| description | VARCHAR(255) | 配置说明 |
| is_secret | INT | 是否敏感 |
| update_by | INT | 更新人 |
| update_time | DATETIME | 更新时间 |

LLM 配置项：

- `LLM_ENABLED`
- `LLM_ACTIVE_PROFILE`
- `LLM_PROFILES`
- 兼容旧配置：`LLM_API_KEY`、`LLM_API_BASE_URL`、`LLM_MODEL`

### 3.3 project_info

| 字段 | 类型 | 说明 |
|---|---|---|
| id | INT PK | 项目ID |
| project_no | VARCHAR(20) UNIQUE | 项目编号，格式 `PRJ-YYYY-NNNN` |
| name | VARCHAR(200) | 项目名称 |
| customer | VARCHAR(200) | 客户名称，兼容字段，默认等于甲方 |
| party_a | VARCHAR(200) | 甲方 |
| party_b | VARCHAR(200) | 乙方 |
| amount | DECIMAL(15,2) | 合同金额 |
| contract_no | VARCHAR(50) NULL | 合同编号，允许为空 |
| sign_date | DATE | 签订日期 |
| project_type | VARCHAR(50) | 项目类型 |
| pm_id | INT | 项目经理ID |
| start_date/end_date | DATE | 计划起止日期 |
| description | TEXT | 项目说明 |
| extra_cost | DECIMAL(15,2) | 后期支出 |
| cost_desc | VARCHAR(500) | 支出说明 |
| status | VARCHAR(20) | draft/pending/approved/active/closing/closed |
| balance_status | VARCHAR(20) | 待回款/部分回款/已结清 |
| create_by | INT | 创建人 |

### 3.4 project_contract

| 字段 | 类型 | 说明 |
|---|---|---|
| id | INT PK | 合同ID |
| project_id | INT FK | 项目ID |
| version | INT | 合同版本 |
| file_type | VARCHAR(10) | word/pdf/image |
| file_path | VARCHAR(500) | 文件路径 |
| file_name | VARCHAR(200) | 原文件名 |
| file_size | BIGINT | 文件大小 |
| upload_by | INT | 上传人 |
| upload_time | DATETIME | 上传时间 |

### 3.5 project_contract_diff

| 字段 | 类型 | 说明 |
|---|---|---|
| id | INT PK | 差异ID |
| project_id | INT FK | 项目ID |
| contract_id | INT FK NULL | 合同ID |
| field_name | VARCHAR(50) | 字段名 |
| field_label | VARCHAR(100) | 字段显示名 |
| registered_value | TEXT | 登记值 |
| recognized_value | TEXT | 识别值 |
| adopted_value | TEXT | 人工采用值 |
| diff_status | VARCHAR(20) | pending/confirmed/ignored |
| confirm_by | INT | 确认人 |
| confirm_time | DATETIME | 确认时间 |
| remark | VARCHAR(500) | 备注 |

### 3.6 project_invoice

| 字段 | 类型 | 说明 |
|---|---|---|
| id | INT PK | 发票ID |
| project_id | INT FK | 项目ID |
| invoice_no | VARCHAR(50) UNIQUE | 发票号码 |
| amount | DECIMAL(15,2) | 价税合计 |
| amount_without_tax | DECIMAL(15,2) | 不含税金额 |
| tax_rate | DECIMAL(5,2) | 税率 |
| tax_amount | DECIMAL(15,2) | 税额 |
| invoice_date | DATE | 开票日期 |
| invoice_type | VARCHAR(20) | 发票类型 |
| buyer | VARCHAR(200) | 购方 |
| seller | VARCHAR(200) | 销方 |
| file_path | VARCHAR(500) | 发票文件 |
| create_by | INT | 创建人 |
| create_time | DATETIME | 创建时间 |

### 3.7 project_payment

| 字段 | 类型 | 说明 |
|---|---|---|
| id | INT PK | 回款ID |
| project_id | INT FK | 项目ID |
| invoice_id | INT FK NULL | 历史兼容字段，允许为空 |
| amount | DECIMAL(15,2) | 回款金额 |
| payment_date | DATE | 回款日期 |
| payment_method | VARCHAR(20) | 回款方式 |
| voucher_file | VARCHAR(500) | 回款凭证 |
| remark | VARCHAR(500) | 备注 |
| create_by | INT | 创建人 |
| create_time | DATETIME | 创建时间 |

最终规则：回款按项目登记，不强制关联具体发票；项目累计回款不得超过项目累计开票价税合计。

### 3.8 project_close

| 字段 | 类型 | 说明 |
|---|---|---|
| id | INT PK | 结项ID |
| project_id | INT FK | 项目ID |
| actual_start | DATE | 实际开始时间 |
| close_time | DATE | 结项时间 |
| report_file | VARCHAR(500) | 验收报告 |
| attachment | VARCHAR(500) | 附件 |
| description | TEXT | 结项说明 |
| status | VARCHAR(20) | pending/approved/rejected |
| balance_status | VARCHAR(20) | 尾款状态 |
| create_by | INT | 创建人 |

### 3.9 审批相关表

| 表 | 说明 |
|---|---|
| approval_template | 审批模板，按 business_type 区分 |
| approval_node | 审批节点，包含节点顺序和超时配置 |
| approval_approver | 审批人配置，支持角色审批 |
| approval_instance | 审批实例，绑定业务类型和业务ID |
| approval_task | 审批任务，形成待办 |
| approval_record | 审批历史记录 |

### 3.10 ocr_recognition_log

| 字段 | 类型 | 说明 |
|---|---|---|
| id | INT PK | 日志ID |
| file_path | VARCHAR(500) | 文件路径 |
| file_name | VARCHAR(200) | 文件名 |
| recognition_type | VARCHAR(20) | contract/invoice/payment |
| engine | VARCHAR(20) | parser/paddleocr |
| raw_result | TEXT | OCR原始摘要，避免超大JSON |
| extracted_info | TEXT | 抽取结果 |
| confidence | DECIMAL(5,4) | 置信度 |
| status | VARCHAR(20) | success/manual_required/failed |
| duration | DECIMAL(10,3) | 耗时 |
| error_message | VARCHAR(500) | 错误信息 |
| create_time | DATETIME | 创建时间 |

## 4. 索引与约束

- `sys_user.username` 唯一。
- `project_info.project_no` 唯一。
- `project_info.status`、`project_info.customer`、`project_info.party_a` 建索引。
- `project_invoice.invoice_no` 唯一。
- `project_invoice.invoice_date` 建索引。
- `project_payment.payment_date` 建索引。
- 审批表按 `business_type`、`business_id`、`status` 建索引。
- OCR 日志按 `recognition_type`、`engine`、`status`、`create_time` 建索引。

## 5. 数据一致性规则

- 项目删除按外键级联清理合同、差异、发票、回款、结项和审批相关数据。
- 发票删除前需检查删除后项目累计回款是否超过剩余累计开票价税合计。
- 已结项项目只读，不允许继续开票。
- 金额校验必须在事务中完成。
- 系统配置中的 API Key 不在接口和日志中明文返回。

## 6. 初始化数据

默认用户：

| 角色 | 用户名 | 密码 |
|---|---|---|
| 管理员 | admin | 123456 |
| 商务 | business01 | 123456 |
| 财务 | finance01 | 123456 |
| 项目经理 | pm01 | 123456 |

默认初始化角色、项目类型字典、审批模板和 LLM 默认配置。
