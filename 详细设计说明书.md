# 智能项目管理系统 - 详细设计说明书

## 1. 引言

### 1.1 编写目的
本文档是智能项目管理系统的详细设计说明书，旨在描述各模块的详细设计，包括类设计、接口设计、算法设计和流程设计，为编码实现提供详细指导。

### 1.2 项目背景
基于概要设计，对各模块进行详细设计，包括数据库表结构、API接口、业务逻辑和前端组件设计。

---

## 2. 用户管理模块详细设计

### 2.1 模块结构

```
用户管理模块
├── 用户管理
│   ├── 用户列表
│   ├── 用户新增
│   ├── 用户编辑
│   └── 用户删除
├── 角色管理
│   ├── 角色列表
│   ├── 角色新增
│   └── 权限分配
├── 数据字典
│   ├── 字典类型
│   └── 字典项
└── 操作日志
    └── 日志查询
```

### 2.2 类设计

#### 用户实体类 (User)
```python
class User(BaseModel):
    id: int
    username: str
    password: str  # BCrypt加密
    name: str
    phone: str
    email: Optional[str]
    role: str  # admin/business/finance/pm，单用户仅允许一个角色
    dept: Optional[str]
    status: int  # 0禁用/1启用
    create_time: datetime
    update_time: datetime
```

#### 角色实体类 (Role)
```python
class Role(BaseModel):
    id: int
    role_name: str
    role_code: str
    permissions: str  # JSON格式权限列表
    status: int
    create_time: datetime
```

### 2.3 接口设计

#### 用户登录接口
```
POST /api/v1/user/login

请求参数：
{
    "username": "string",
    "password": "string"
}

响应参数：
{
    "code": 200,
    "message": "success",
    "data": {
        "token": "string",
        "user": {
            "id": 1,
            "username": "admin",
            "name": "管理员",
            "role": "admin"
        }
    }
}
```

#### 用户列表接口
```
GET /api/v1/user/list?page=1&size=10&keyword=&role=

响应参数：
{
    "code": 200,
    "data": {
        "total": 100,
        "list": [
            {
                "id": 1,
                "username": "admin",
                "name": "管理员",
                "phone": "13800138000",
                "role": "admin",
                "status": 1
            }
        ]
    }
}
```

### 2.4 业务逻辑

#### 登录流程
```
┌─────────┐
│  开始   │
└────┬────┘
     ▼
┌─────────────────┐
│  参数校验       │
│  (用户名/密码)  │
└────┬────────────┘
     ▼
┌─────────────────┐
│  查询用户       │
└────┬────────────┘
     ▼
┌─────────────────┐
│  密码验证       │
│  (BCrypt)       │
└────┬────────────┘
     ▼
┌─────────────────┐
│  生成JWT Token  │
└────┬────────────┘
     ▼
┌─────────────────┐
│  返回用户信息   │
└────┬────────────┘
     ▼
└─────────┐
│  结束   │
└─────────┘
```

---

## 3. 立项管理模块详细设计

### 3.1 模块结构

```
立项管理模块
├── 立项前登记
│   ├── Word合同上传
│   ├── 文档解析
│   └── NLP信息提取
├── 立项信息登记
│   ├── 基本信息
│   ├── PDF合同上传
│   ├── 后期支出
│   └── 合同校验
├── 合同差异确认
│   ├── 差异列表
│   ├── 人工采用值
│   └── 确认备注
├── 立项审核
│   ├── 单个审核
│   └── 批量审核
└── 项目列表
    ├── 查询筛选
    ├── 批量导出
    └── 项目操作
```

### 3.2 类设计

#### 项目实体类 (Project)
```python
class Project(BaseModel):
    id: int
    project_no: str  # PRJ-YYYY-NNNN
    name: str
    customer: str
    amount: Decimal
    contract_no: str
    sign_date: date
    project_type: str
    pm_id: int
    start_date: date
    end_date: date
    description: Optional[str]
    status: str  # draft/pending/approved/active/closed/terminated/framework
    balance_status: str  # 已结清/部分回款/待回款
    create_by: int
    create_time: datetime
    update_time: datetime
```

#### 合同实体类 (Contract)
```python
class Contract(BaseModel):
    id: int
    project_id: int
    version: int
    file_type: str  # word/pdf
    file_path: str
    upload_time: datetime
```

#### 合同差异确认类 (ContractDiff)
```python
class ContractDiff(BaseModel):
    id: int
    project_id: int
    contract_id: Optional[int]
    field_name: str
    field_label: str
    registered_value: Optional[str]
    recognized_value: Optional[str]
    adopted_value: Optional[str]
    diff_status: str  # pending/confirmed/ignored
    confirm_by: Optional[int]
    confirm_time: Optional[datetime]
    remark: Optional[str]
    create_time: datetime
```

#### OCR识别结果类 (OcrResult)
```python
class OcrResult(BaseModel):
    raw_text: list
    extracted_info: dict
    confidence: dict
    engine: str  # parser/paddleocr/baidu
    duration: float
```

### 3.3 接口设计

#### 创建项目接口
```
POST /api/v1/project/create
Content-Type: multipart/form-data

请求参数：
{
    "name": "string",
    "customer": "string",
    "amount": 500000,
    "contract_no": "string",
    "sign_date": "2024-01-15",
    "project_type": "software",
    "pm_id": 1,
    "start_date": "2024-01-20",
    "end_date": "2024-06-30",
    "description": "string",
    "extra_cost": 0,
    "cost_desc": "string",
    "word_contract": "file(.doc/.docx)"
}

响应参数：
{
    "code": 200,
    "data": {
        "id": 1,
        "project_no": "PRJ-2024-0001"
    }
}
```

说明：创建项目接口直接接收业务表单和 Word 合同附件。`.doc/.docx` 先走文档解析获取文本，再通过 NLP 提取项目名称、合同金额、合同编号、签订日期等字段。

#### 合同差异确认接口
```
POST /api/v1/project/contract-diff/confirm

请求参数：
{
    "diff_id": 1,
    "adopted_value": "2024-01-15",
    "diff_status": "confirmed",
    "remark": "采用登记值"
}

响应参数：
{
    "code": 200,
    "message": "确认成功"
}
```

#### 管理员确认项目开始接口
```
POST /api/v1/project/start

请求参数：
{
    "project_id": 1,
    "remark": "项目已具备执行条件"
}

响应参数：
{
    "code": 200,
    "message": "项目已进入进行中"
}
```

#### 批量审核接口
```
POST /api/v1/project/batch-approve

请求参数：
{
    "project_ids": [1, 2, 3],
    "result": "approved",  # approved/rejected
    "opinion": "string",
    "reason": "string"  # 驳回时必填
}

响应参数：
{
    "code": 200,
    "message": "批量审核成功",
    "data": {
        "success_count": 3,
        "fail_count": 0
    }
}
```

### 3.4 业务逻辑

#### 立项流程
```
┌─────────────────────────────────────────────────────────────┐
│                      立项流程详细设计                        │
└─────────────────────────────────────────────────────────────┘

1. 上传Word合同
   ├── 文件类型校验 (.doc/.docx)
   ├── 文件大小校验 (≤20MB)
   ├── 文档解析提取正文文本
   ├── 调用NLP提取关键信息
   │   ├── 成功 → 自动填充表单
   │   └── 部分成功/失败 → 标记字段并允许人工填写
   └── 返回识别结果

2. 立项信息登记
   ├── 填充文档解析+NLP提取结果
   ├── 人工修正确认
   ├── 上传PDF合同 (支持多版本)
   ├── 记录后期支出
   └── 保存草稿

3. 合同校验
   ├── PDF合同OCR识别
   ├── OCR文本进入NLP提取
   ├── 与录入信息比对
   ├── 生成差异清单并写入project_contract_diff
   └── 人工确认差异，保存采用值、确认人、确认时间和备注

4. 提交审批
   ├── 创建审批实例和审批任务
   └── 更新项目状态为"待审核"

5. 审批处理
   ├── 通过 → 状态变为"已立项"
   └── 驳回 → 状态回到"草稿"

6. 管理员确认项目开始
   ├── 校验项目状态为"已立项"
   ├── 管理员执行确认
   └── 状态变为"进行中"
```

#### 项目编号生成算法
```python
def generate_project_no():
    """生成项目编号: PRJ-YYYY-NNNN"""
    year = datetime.now().year
    # 查询当年最大编号
    max_no = db.query(
        func.max(Project.project_no)
    ).filter(
        Project.project_no.like(f'PRJ-{year}-%')
    ).scalar()
    
    if max_no:
        seq = int(max_no.split('-')[2]) + 1
    else:
        seq = 1
    
    return f'PRJ-{year}-{seq:04d}'
```

---

## 4. 开票管理模块详细设计

### 4.1 模块结构

```
开票管理模块
├── 开票登记
│   ├── 选择项目
│   ├── 发票上传
│   ├── OCR识别
│   └── 信息确认
├── 开票记录
│   ├── 记录列表
│   └── 记录详情
└── 开票统计
    ├── 开票汇总
    └── 阈值提醒
```

### 4.2 类设计

#### 发票实体类 (Invoice)
```python
class Invoice(BaseModel):
    id: int
    project_id: int
    invoice_no: str
    amount: Decimal
    invoice_date: date
    invoice_type: str  # special/normal
    buyer: str
    seller: str
    file_path: str
    create_by: int
    create_time: datetime
```

### 4.3 接口设计

#### 开票登记接口
```
POST /api/v1/invoice/create
Content-Type: multipart/form-data

请求参数：
{
    "project_id": 1,
    "invoice_no": "string",
    "amount": 100000,
    "invoice_date": "2024-02-15",
    "invoice_type": "special",
    "buyer": "string",
    "seller": "string",
    "invoice_file": "file(PDF/图片)",
    "delivery_file": "file(可选)"
}

响应参数：
{
    "code": 200,
    "data": {
        "id": 1
    }
}
```

### 4.4 业务逻辑

#### 开票金额校验
```python
def validate_invoice_amount(project_id: int, invoice_amount: Decimal):
    """校验开票金额"""
    with db.transaction():
        # 锁定项目行，避免并发开票同时通过校验
        project = db.query(Project).filter(
            Project.id == project_id
        ).with_for_update().one()

        if project.status not in ["approved", "active"]:
            raise BusinessError("仅已立项或进行中项目允许开票")

        # 查询已开票金额
        invoiced_amount = db.query(
            func.sum(Invoice.amount)
        ).filter(
            Invoice.project_id == project_id
        ).scalar() or Decimal("0")

        # 校验是否超过合同金额
        if project.amount > 0 and invoiced_amount + invoice_amount > project.amount:
            raise BusinessError("开票金额超过合同金额")

        # 检查是否触发审批阈值 (80%)
        threshold = project.amount * Decimal("0.8")
        if project.amount > 0 and invoiced_amount + invoice_amount >= threshold:
            create_approval_instance("invoice", project_id)

        return True
```

---

## 5. 回款管理模块详细设计

### 5.1 模块结构

```
回款管理模块
├── 回款登记
│   ├── 选择项目
│   ├── 选择关联发票
│   ├── 回款信息
│   └── 凭证上传
├── 回款记录
│   ├── 记录列表
│   └── 记录详情
└── 应收款计算
    ├── 自动计算
    └── 尾款状态
```

### 5.2 类设计

#### 回款实体类 (Payment)
```python
class Payment(BaseModel):
    id: int
    project_id: int
    invoice_id: int
    amount: Decimal
    payment_date: date
    payment_method: str  # bank/check/cash
    voucher_file: Optional[str]
    remark: Optional[str]
    create_by: int
    create_time: datetime
```

### 5.3 接口设计

#### 回款登记接口
```
POST /api/v1/payment/create
Content-Type: multipart/form-data

请求参数：
{
    "project_id": 1,
    "invoice_id": 1,
    "amount": 50000,
    "payment_date": "2024-03-10",
    "payment_method": "bank",
    "remark": "首期回款",
    "voucher_file": "file(可选)"
}

响应参数：
{
    "code": 200,
    "data": {
        "id": 1
    }
}
```

### 5.4 业务逻辑

#### 回款金额校验
```python
def validate_payment_amount(project_id: int, invoice_id: int, payment_amount: Decimal):
    """校验回款金额"""
    with db.transaction():
        # 业务规则：每笔回款必须绑定单张发票，且只能冲抵该发票
        invoice = db.query(Invoice).filter(
            Invoice.id == invoice_id,
            Invoice.project_id == project_id
        ).with_for_update().one_or_none()

        if not invoice:
            raise BusinessError("关联发票不存在或不属于该项目")

        paid_amount = db.query(
            func.sum(Payment.amount)
        ).filter(
            Payment.invoice_id == invoice_id
        ).scalar() or Decimal("0")

        if paid_amount + payment_amount > invoice.amount:
            raise BusinessError("回款金额超过关联发票金额")

        return True
```

#### 应收款计算
```python
def calculate_receivable(project_id: int):
    """计算应收款"""
    project = get_project(project_id)
    
    # 查询已开票金额
    invoiced_amount = db.query(
        func.sum(Invoice.amount)
    ).filter(
        Invoice.project_id == project_id
    ).scalar() or 0
    
    # 查询已回款金额
    paid_amount = db.query(
        func.sum(Payment.amount)
    ).filter(
        Payment.project_id == project_id
    ).scalar() or 0
    
    # 计算应收款
    receivable = invoiced_amount - paid_amount
    
    # 更新尾款状态
    if paid_amount >= project.amount:
        balance_status = "已结清"
    elif paid_amount > 0:
        balance_status = "部分回款"
    else:
        balance_status = "待回款"
    
    return {
        "invoiced_amount": invoiced_amount,
        "paid_amount": paid_amount,
        "receivable": receivable,
        "balance_status": balance_status
    }
```

---

## 6. 结项管理模块详细设计

### 6.1 模块结构

```
结项管理模块
├── 结项申请
│   ├── 选择项目
│   ├── 验收报告
│   ├── 结项说明
│   └── 提交申请
├── 结项审核
│   ├── 单个审核
│   └── 批量审核
├── 结项撤回
│   └── 撤回处理
└── 尾款状态
    └── 状态标记
```

### 6.2 类设计

#### 结项实体类 (ProjectClose)
```python
class ProjectClose(BaseModel):
    id: int
    project_id: int
    actual_start: date
    close_time: date
    report_file: Optional[str]
    description: str
    status: str  # pending/closed/rejected
    balance_status: str  # 已结清/部分回款/待回款
    create_by: int
    create_time: datetime
    update_time: datetime
```

### 6.3 接口设计

#### 结项申请接口
```
POST /api/v1/close/apply
Content-Type: multipart/form-data

请求参数：
{
    "project_id": 1,
    "actual_start": "2024-01-20",
    "close_time": "2024-06-30",
    "description": "string",
    "report_file": "file(可选)",
    "attachment": "file(可选)"
}

响应参数：
{
    "code": 200,
    "data": {
        "id": 1
    }
}
```

#### 撤回结项接口
```
POST /api/v1/close/withdraw

请求参数：
{
    "project_id": 1,
    "reason": "string"
}

响应参数：
{
    "code": 200,
    "message": "撤回成功"
}
```

### 6.4 业务逻辑

#### 结项流程
```
┌─────────────────────────────────────────────────────────────┐
│                      结项流程详细设计                        │
└─────────────────────────────────────────────────────────────┘

1. 结项申请
   ├── 校验项目状态 (已立项/进行中)
   ├── 记录结项时间
   ├── 上传验收报告 (可选)
   ├── 填写结项说明
   ├── 创建结项记录
   └── 创建结项审批实例和财务审批任务

2. 结项审核
   ├── 财务审核
   │   ├── 通过 → 项目状态变为"已结项"，项目只读且不可再开票
   │   └── 驳回 → 退回项目经理
   └── 更新尾款状态

3. 结项撤回
   ├── 仅管理员可操作
   ├── 校验项目状态 (已结项)
   ├── 记录撤回原因
   ├── 项目状态回到"进行中"
   └── 保留结项和审批历史记录
```

---

## 7. 审批流模块详细设计

### 7.1 模块结构

```
审批流模块
├── 审批流配置
│   ├── 审批模板
│   ├── 审批节点
│   └── 审批人配置
├── 审批实例
│   ├── 实例创建
│   ├── 当前节点
│   └── 实例状态
├── 审批处理
│   ├── 待办列表
│   ├── 审批操作
│   └── 批量审批
└── 审批记录
    └── 记录查询
```

### 7.2 类设计

#### 审批模板类 (ApprovalTemplate)
```python
class ApprovalTemplate(BaseModel):
    id: int
    template_name: str
    business_type: str  # project/invoice/close
    status: int  # 0禁用/1启用
    create_time: datetime
```

#### 审批节点类 (ApprovalNode)
```python
class ApprovalNode(BaseModel):
    id: int
    template_id: int
    node_name: str
    node_order: int
    approval_type: str  # 会签/或签
    timeout_hours: int
```

#### 审批实例类 (ApprovalInstance)
```python
class ApprovalInstance(BaseModel):
    id: int
    business_type: str  # project/invoice/close
    business_id: int
    template_id: int
    current_node_id: Optional[int]
    status: str  # pending/approved/rejected/cancelled
    start_by: int
    start_time: datetime
    finish_time: Optional[datetime]
    remark: Optional[str]
```

#### 审批任务类 (ApprovalTask)
```python
class ApprovalTask(BaseModel):
    id: int
    instance_id: int
    node_id: int
    approver_id: int
    status: str  # pending/approved/rejected/cancelled
    opinion: Optional[str]
    reason: Optional[str]
    create_time: datetime
    approve_time: Optional[datetime]
```

#### 审批记录类 (ApprovalRecord)
```python
class ApprovalRecord(BaseModel):
    id: int
    instance_id: int
    task_id: int
    business_type: str
    business_id: int
    template_id: int
    node_id: int
    approver_id: int
    status: str  # pending/approved/rejected/cancelled
    opinion: Optional[str]
    create_time: datetime
    update_time: datetime
```

### 7.3 接口设计

#### 审批流配置接口
```
POST /api/v1/approval/template/create

请求参数：
{
    "template_name": "立项审批",
    "business_type": "project",
    "nodes": [
        {
            "node_name": "管理员审核",
            "node_order": 1,
            "approval_type": "或签",
            "timeout_hours": 48,
            "approvers": [
                {"type": "role", "id": "admin"}
            ]
        }
    ]
}
```

#### 审批处理接口
```
POST /api/v1/approval/process

请求参数：
{
    "task_id": 1,
    "result": "approved",  # approved/rejected
    "opinion": "string",
    "reason": "string"  # 驳回时必填
}
```

### 7.4 业务逻辑

#### 审批流程
```
┌─────────────────────────────────────────────────────────────┐
│                      审批流程详细设计                        │
└─────────────────────────────────────────────────────────────┘

1. 创建审批实例和任务
   ├── 查询审批模板
   ├── 获取第一个审批节点
   ├── 获取审批人
   ├── 创建approval_instance
   └── 创建approval_task待办

2. 审批处理
   ├── 校验审批人权限
   ├── 更新审批任务状态
   ├── 写入approval_record历史记录
   ├── 判断是否还有下一节点
   │   ├── 有 → 创建下一节点待办
   │   └── 无 → 完成审批，更新业务状态
   └── 记录审批日志

3. 当前版本边界
   ├── 不设计消息表、通知表和定时任务记录表
   ├── 待办数量由审批任务表实时查询
   └── 超时字段保留在审批节点配置中，当前版本不落地自动催办/自动转交
```

---

## 8. OCR服务模块详细设计

### 8.1 模块结构

```
OCR服务模块
├── 文档解析
│   └── Word合同解析
├── PaddleOCR识别
│   ├── PDF/图片合同识别
│   └── 发票识别
├── 百度OCR兜底
│   ├── PDF/图片合同识别
│   └── 发票识别
└── 识别日志
    └── 日志记录
```

### 8.2 类设计

#### OCR服务类 (OcrService)
```python
class OcrService:
    def __init__(self):
        self.paddle_ocr = PaddleOCR(use_angle_cls=True, lang="ch")
        self.baidu_client = BaiduOcrClient()
    
    async def recognize_contract(self, file_path: str, file_ext: str) -> OcrResult:
        """合同信息提取：Word走文档解析+NLP，PDF/图片走OCR+NLP"""
        if file_ext in [".doc", ".docx"]:
            text = await self._parse_word(file_path)
            return await self._nlp_extract_contract(text)

        try:
            # PDF/图片优先使用PaddleOCR
            result = await self._paddle_recognize(file_path)
            if result.confidence >= 0.8:
                return result
        except Exception as e:
            logger.warning(f"PaddleOCR failed: {e}")
        
        # 兜底使用百度OCR
        return await self._baidu_recognize(file_path)
    
    async def recognize_invoice(self, file_path: str) -> OcrResult:
        """发票识别"""
        # 类似合同识别逻辑
        pass
```

### 8.3 接口设计

#### 合同识别接口
```
POST /api/v1/ocr/contract

请求参数：
- file: 上传文件 (multipart/form-data)
- type: contract

处理规则：
- `.doc/.docx`：文档解析 + NLP 提取，不调用 OCR
- `.pdf/.jpg/.png`：PaddleOCR + NLP，低置信度或失败时使用百度OCR兜底

响应参数：
{
    "code": 200,
    "data": {
        "raw_text": [...],
        "extracted_info": {
            "project_name": "XX系统开发项目",
            "contract_amount": "500000",
            "contract_no": "HT-2024-001",
            "sign_date": "2024-01-15",
            "party_a": "XX科技有限公司"
        },
        "confidence": {
            "project_name": 0.98,
            "contract_amount": 0.99,
            "contract_no": 0.92,
            "sign_date": 0.96,
            "party_a": 0.94
        },
        "engine": "paddleocr",
        "duration": 1.2
    }
}
```

### 8.4 业务逻辑

#### OCR识别流程
```
┌─────────────────────────────────────────────────────────────┐
│                      OCR识别流程详细设计                      │
└─────────────────────────────────────────────────────────────┘

1. 文件预处理
   ├── 文件类型校验
   ├── 文件大小校验
   └── 判断处理路径

2. Word合同处理
   ├── 解析.doc/.docx正文文本
   ├── 调用NLP提取关键信息
   └── 返回字段值与置信度

3. PDF/图片OCR识别
   ├── 调用PaddleOCR API
   ├── 提取文字和坐标
   ├── 计算置信度
   └── 调用NLP提取关键信息

4. 置信度判断
   ├── ≥80% → 返回识别结果
   └── <80% → 尝试百度OCR

5. 百度OCR兜底
   ├── 调用百度OCR API
   ├── 提取文字和置信度
   └── 返回识别结果

6. 结果处理
   ├── 记录识别日志
   └── 返回最终结果
```

---

## 9. 查询汇总模块详细设计

### 9.1 模块结构

```
查询汇总模块
├── 数据查询
│   ├── 项目查询
│   ├── 开票查询
│   └── 回款查询
├── 数据可视化
│   ├── 趋势图
│   ├── 分布图
│   └── 对比图
└── 报表导出
    ├── Excel导出
    ├── PDF导出
    └── 批量导出
```

### 9.2 接口设计

#### 数据概览接口
```
GET /api/v1/statistics/overview?year=2024&month=1

响应参数：
{
    "code": 200,
    "data": {
        "total_projects": 156,
        "active_projects": 42,
        "total_contract_amount": 12800000,
        "total_invoice_amount": 8500000,
        "total_payment_amount": 6200000,
        "total_receivable": 2300000
    }
}
```

#### 批量导出接口
```
POST /api/v1/export/batch

请求参数：
{
    "project_ids": [1, 2, 3],
    "export_types": ["project", "invoice", "payment"],
    "format": "excel"
}

响应参数：
{
    "code": 200,
    "data": {
        "task_id": "export_20240116_001",
        "status": "processing"
    }
}
```

说明：`task_id` 是运行时任务队列中的临时任务标识，不写入数据库。系统不提供持久化导出历史，服务重启后未完成任务可视为失败或需重新发起。

#### 导出状态查询接口
```
GET /api/v1/export/status?task_id=export_20240116_001

响应参数：
{
    "code": 200,
    "data": {
        "task_id": "export_20240116_001",
        "status": "finished",
        "download_url": "/downloads/export_20240116_001.xlsx"
    }
}
```

### 9.3 业务逻辑

#### 批量导出流程
```
┌─────────────────────────────────────────────────────────────┐
│                      批量导出流程详细设计                      │
└─────────────────────────────────────────────────────────────┘

1. 创建导出任务
   ├── 校验参数
   ├── 写入运行时任务队列
   └── 返回临时任务ID

2. 异步处理
   ├── 查询数据
   ├── 生成Excel/PDF
   ├── 保存文件
   └── 更新运行时队列中的任务状态

3. 结果获取
   ├── 前端通过任务ID查询导出状态
   └── 导出完成后提供下载链接

4. 持久化边界
   ├── 不设计导出任务表
   ├── 不保留导出历史记录
   └── 运行时任务状态随服务进程生命周期维护
```

---

## 10. 前端组件设计

### 10.1 组件结构

```
前端组件结构
├── 布局组件
│   ├── Layout
│   ├── Sidebar
│   └── Header
├── 通用组件
│   ├── SearchForm
│   ├── DataTable
│   ├── DetailDialog
│   └── ExportDialog
├── 业务组件
│   ├── OcrUpload
│   ├── ApprovalFlow
│   ├── DiffTable
│   └── BalanceStatus
└── 页面组件
    ├── Dashboard
    ├── ProjectList
    ├── InvoiceList
    ├── PaymentList
    ├── CloseList
    ├── Statistics
    └── System
```

### 10.2 组件设计

#### OCR上传组件 (OcrUpload)
```vue
<template>
  <div class="ocr-upload">
    <el-upload
      :action="uploadUrl"
      :before-upload="beforeUpload"
      :on-success="handleSuccess"
      :accept="acceptTypes"
    >
      <el-button type="primary">选择文件</el-button>
      <template #tip>
        <div class="el-upload__tip">{{ tipText }}</div>
      </template>
    </el-upload>
    
    <div v-if="ocrResult" class="ocr-result">
      <div class="ocr-title">
        <span>✅</span> PaddleOCR识别完成
        <el-tag size="small" type="success">识别引擎: PaddleOCR</el-tag>
      </div>
      <div class="ocr-fields">
        <div v-for="(value, key) in ocrResult.extracted_info" :key="key" class="ocr-field">
          <span class="ocr-label">{{ fieldLabels[key] }}：</span>
          <span class="ocr-value">{{ value }}</span>
          <el-tag 
            size="small" 
            :type="ocrResult.confidence[key] >= 0.95 ? 'success' : 'warning'"
          >
            置信度: {{ (ocrResult.confidence[key] * 100).toFixed(0) }}%
          </el-tag>
        </div>
      </div>
    </div>
  </div>
</template>
```

#### 批量审批组件 (BatchApproval)
```vue
<template>
  <el-dialog title="批量审核" width="600px">
    <el-alert
      :title="'已选择 ' + pendingCount + ' 个待审核项目'"
      type="info"
      show-icon
      :closable="false"
    />
    
    <el-table :data="pendingList" style="margin: 20px 0;">
      <!-- 表格列 -->
    </el-table>
    
    <el-form :model="form">
      <el-form-item label="审批结果">
        <el-radio-group v-model="form.result">
          <el-radio label="approved">批量通过</el-radio>
          <el-radio label="rejected">批量驳回</el-radio>
        </el-radio-group>
      </el-form-item>
      <el-form-item label="审批意见">
        <el-input v-model="form.opinion" type="textarea" />
      </el-form-item>
    </el-form>
    
    <template #footer>
      <el-button @click="cancel">取消</el-button>
      <el-button type="primary" @click="submit">确认提交</el-button>
    </template>
  </el-dialog>
</template>
```

---

## 11. 附录

### 11.1 参考文档
- 概要设计说明书
- 数据库设计说明书
- 需求说明书
