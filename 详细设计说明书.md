# 智能项目管理系统详细设计说明书（最终版）

## 1. 引言

本文档描述系统最终版各模块的详细设计，包括实体模型、接口、业务逻辑、OCR/LLM 解析、财务校验、审批流和前端页面设计。

## 2. 用户与权限模块

### 2.1 实体设计

`sys_user` 保存用户账号、BCrypt 密码哈希、姓名、手机号、邮箱、单一角色、部门和状态。系统不支持一个用户多个角色。

### 2.2 登录流程

```text
提交用户名密码 -> 查询启用用户 -> BCrypt校验 -> 生成JWT -> 返回token和用户信息
```

### 2.3 权限判断

- 普通角色按接口要求校验。
- 管理员 `admin` 作为超级角色，默认通过业务接口角色校验。
- 前端菜单只控制展示，后端接口负责真正权限限制。

### 2.4 主要接口

| 接口 | 方法 | 说明 |
|---|---|---|
| `/api/v1/user/login` | POST | 登录 |
| `/api/v1/user/list` | GET | 用户列表，支持关键字、角色、状态筛选 |
| `/api/v1/user/create` | POST | 新增用户 |
| `/api/v1/user/update` | PUT | 编辑用户 |
| `/api/v1/user/{user_id}/status` | PUT | 启用/禁用 |
| `/api/v1/user/{user_id}` | DELETE | 删除用户 |

## 3. 立项管理模块

### 3.1 模块组成

```text
立项管理
├── Word/.doc 合同解析
├── 草稿保存与刷新恢复
├── 盖章合同 PDF/图片 OCR
├── 合同差异生成
├── 合同差异确认
├── 提交审核
├── 管理员审核/驳回
└── 管理员确认开始
```

### 3.2 合同解析设计

#### Word/.doc 合同

- `.docx` 直接解析段落和表格文本。
- `.doc` 通过 LibreOffice 转换后解析。
- 解析后使用规则抽取项目名称、合同金额、合同编号、签订日期、甲方、乙方、丙方、项目类型。

#### PDF/图片合同

- 上传文件保存到 uploads。
- PDF 在 PaddleOCR 服务内部转图片逐页识别。
- OCR 文本进入规则抽取。
- 识别值与登记值生成差异。

#### LLM 兜底

- 系统管理页可配置多个 OpenAI 兼容模型。
- 当前启用模型保存于数据库配置。
- `LLM_ENABLED=false` 或 API Key 为空时不调用 LLM。
- LLM 仅用于弱标识合同补缺或改善项目名。
- 规则明确识别出的合同总金额、甲乙方和日期优先，不被 LLM 覆盖。

### 3.3 字段规则

| 字段 | 规则 |
|---|---|
| project_no | 系统生成，格式 `PRJ-YYYY-NNNN` |
| contract_no | 允许为空；项目编号、招标编号、采购编号不作为合同编号 |
| contract_amount | 优先取总金额标签，不取分期金额、日期、账号或编号 |
| party_a/customer | 甲方，customer 兼容等于 party_a |
| party_b | 乙方 |
| party_c | 丙方，可为空 |
| project_type | 可由合同解析结果自动扩展字典 |

### 3.4 合同差异确认

差异数据保存：

- 登记值 `registered_value`
- 识别值 `recognized_value`
- 人工采用值 `adopted_value`
- 确认人 `confirm_by`
- 确认时间 `confirm_time`
- 备注 `remark`

确认差异后，采用值写回项目主表，并刷新项目列表和详情。

### 3.5 主要接口

| 接口 | 方法 | 说明 |
|---|---|---|
| `/api/v1/project/next-no` | GET | 预览下一个项目编号 |
| `/api/v1/project/word/parse-draft` | POST | 上传 Word/.doc 并解析草稿 |
| `/api/v1/project/draft/save` | POST | 保存草稿 |
| `/api/v1/project/draft/current` | GET | 获取当前草稿 |
| `/api/v1/project/stamped-contract/parse` | POST | 上传盖章合同并生成差异 |
| `/api/v1/project/contract-diff/confirm` | POST | 确认差异 |
| `/api/v1/project/submit` | POST | 提交审核 |
| `/api/v1/project/approve` | POST | 管理员审批 |
| `/api/v1/project/start` | POST | 管理员确认开始 |
| `/api/v1/project/list` | GET | 项目列表，支持关键字、状态、年份 |
| `/api/v1/project/options` | GET | 项目联想搜索 |

## 4. 审批流模块

### 4.1 数据模型

审批流由 `approval_template`、`approval_node`、`approval_approver`、`approval_instance`、`approval_task`、`approval_record` 组成。

### 4.2 审批处理流程

```text
业务提交 -> 创建审批实例 -> 创建审批任务 -> 审批人处理 -> 写审批记录 -> 更新业务状态
```

### 4.3 主要接口

| 接口 | 方法 | 说明 |
|---|---|---|
| `/api/v1/approval/task/list` | GET | 待办任务 |
| `/api/v1/approval/process` | POST | 审批通过/驳回 |
| `/api/v1/approval/flow/current` | GET | 当前用户审批流状态 |
| `/api/v1/approval/template/list` | GET | 审批模板列表 |

## 5. 开票回款模块

### 5.1 页面设计

开票回款保留单个侧边菜单，页面内部使用 Tabs：

- 开票登记
- 回款登记
- 发票记录
- 回款记录
- 台账

项目选择框支持用户输入关键字，后端按项目编号、合同编号、项目名称、甲方/客户模糊搜索并返回候选。

### 5.2 发票设计

发票字段：

- 发票号码
- 开票日期
- 购方
- 销方
- 不含税金额
- 税率
- 税额
- 价税合计
- 发票文件

保存规则：

- 发票号码唯一。
- 发票 `amount` 表示价税合计。
- 开票额度校验按不含税金额累计。
- 已结项项目不可开票。
- 保存后刷新发票记录、回款记录和台账。

### 5.3 回款设计

最终版回款不强制关联单张发票，`invoice_id` 仅保留为历史兼容可空字段。

保存规则：

- 回款按项目登记。
- 项目累计回款 + 本次回款 <= 项目累计开票价税合计。
- OCR 识别不可靠时返回人工确认状态，允许手工填写。
- 删除回款后重新计算台账。

### 5.4 台账设计

台账展示：

- 合同金额
- 累计开票价税合计
- 累计开票不含税金额
- 累计回款
- 未回款金额
- 开票进度
- 回款进度
- 发票明细
- 回款明细
- 异常提示

### 5.5 主要接口

| 接口 | 方法 | 说明 |
|---|---|---|
| `/api/v1/invoice/create` | POST | 开票登记 |
| `/api/v1/invoice/list` | GET | 发票列表 |
| `/api/v1/invoice/{invoice_id}` | DELETE | 删除发票 |
| `/api/v1/payment/create` | POST | 回款登记 |
| `/api/v1/payment/list` | GET | 回款列表 |
| `/api/v1/payment/{payment_id}` | DELETE | 删除回款 |
| `/api/v1/finance/summary` | GET | 台账汇总 |

## 6. 结项管理模块

### 6.1 结项申请

项目经理选择已立项或进行中项目，填写结项时间、说明，上传验收报告和附件。若项目回款未完成，前端提示未回款金额，但允许提交审批。

### 6.2 财务审批

财务可查看：

- 项目基础信息
- 商务提交的立项内容
- 合同差异
- 发票明细
- 回款明细
- 台账和未回款金额
- 项目经理提交的验收报告和结项说明

审批通过后项目状态变为 `closed`，项目只读且不可继续开票。

## 7. 查询报表与导出模块

### 7.1 查询模式

- 年度模式：自然年 1-12 月趋势。
- 月度模式：自然月每日趋势和当月发票/回款明细。
- 关键字匹配项目编号、合同编号、项目名称、甲方/客户。

### 7.2 导出任务

- `/api/v1/export/batch` 创建导出任务。
- `/api/v1/export/status` 查询运行时任务状态。
- `/api/v1/export/download` 下载 `.xlsx`。
- 导出任务不落库，服务重启后未完成任务视为失败。

## 8. 系统管理模块

### 8.1 用户管理

支持用户新增、编辑、启用、禁用、删除、筛选和统计。当前登录管理员不能禁用或删除自己。

### 8.2 LLM 配置

系统配置页支持：

- LLM 总开关。
- 多个模型配置。
- 当前启用模型。
- API Base URL。
- 模型名称。
- API Key 密码输入和掩码显示。
- 测试连接。
- 保存后即时生效。

配置保存在 `sys_config`，读取优先级为数据库配置优先，环境变量兜底。

### 8.3 日志管理

审计日志和 OCR 日志在系统管理页使用固定高度滚动容器展示。OCR 日志保存识别类型、引擎、置信度、状态、摘要结果和错误信息。

## 9. OCR 与规则抽取设计

### 9.1 识别类型

| 类型 | 输入 | 处理方式 |
|---|---|---|
| contract | Word/.doc/PDF/图片合同 | 文档解析或 OCR + 规则 + 可选 LLM |
| invoice | 发票 PDF/图片 | OCR + 发票字段规则抽取 |
| payment | 回款凭证 PDF/图片 | OCR + 本次回款金额优先规则 |

### 9.2 失败策略

- OCR 失败写入 `ocr_recognition_log`。
- 字段缺失返回 `manual_required`。
- 前端允许人工补充，不阻断业务流程。
- 不按样例图片硬编码金额、客户名或固定坐标。

## 10. 测试设计

测试覆盖：

- 登录和权限。
- 用户管理。
- 项目编号生成。
- 合同解析和 LLM 配置。
- 合同差异确认。
- 立项审批通过/驳回。
- 发票税务字段和唯一约束。
- 项目级回款金额校验。
- 结项审批。
- 年度/月度报表。
- 导出下载。

常用命令：

```powershell
python -m compileall backend\app backend\alembic paddleocr
$env:PYTHONPATH='backend'; pytest -q
cd frontend; npm.cmd run build
```
