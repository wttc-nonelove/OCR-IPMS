# 智能项目管理系统概要设计说明书（最终版）

## 1. 引言

### 1.1 编写目的

本文档描述智能项目管理系统最终版的总体架构、模块划分、数据流、接口规范、部署架构和安全设计，为详细设计、测试验收和课程设计报告提供依据。

### 1.2 系统范围

系统覆盖项目立项、合同解析、差异确认、审批流、开票回款、结项审批、报表导出和系统管理。当前最终版以 Docker Compose 部署，前端、后端、数据库和 OCR 服务分离。

## 2. 总体架构

```text
Browser
  |
  v
Vue 3 + Element Plus 前端（Nginx :80）
  |
  v
FastAPI 后端（Uvicorn :8000）
  |------------------ MySQL 8.0（:3306）
  |------------------ PaddleOCR 服务（:8001 -> :8000）
  |------------------ 本地 uploads 文件目录
  |
  +-- OpenAI 兼容 LLM 服务（按系统配置启用）
```

## 3. 技术架构

| 层级 | 技术 | 职责 |
|---|---|---|
| 前端 | Vue 3、TypeScript、Element Plus、Pinia、Vue Router | 页面展示、表单交互、角色菜单、状态管理 |
| 后端 | FastAPI、SQLAlchemy、Pydantic、JWT、BCrypt | REST API、鉴权、业务校验、审批推进、导出 |
| 数据库 | MySQL 8.0 | 业务数据、审批数据、日志、系统配置 |
| OCR | PaddleOCR 独立容器 | PDF/图片识别，PDF转图片逐页识别 |
| LLM | OpenAI兼容接口 | 弱标识合同兜底解析 |
| 文件 | 本地 uploads | 合同、发票、回款凭证、验收报告 |
| 部署 | Docker Compose | 四服务一键启动 |

## 4. 功能模块

| 模块 | 主要职责 |
|---|---|
| 用户与权限 | 登录、JWT鉴权、用户管理、单角色控制、管理员超级权限 |
| 工作台 | 指标、待办、项目生命周期统计、开票回款趋势 |
| 立项管理 | 合同解析、草稿保存、差异确认、立项审批、项目启动 |
| 审批流 | 审批模板、审批实例、审批任务、审批记录 |
| 开票回款 | 开票登记、回款登记、发票记录、回款记录、台账 |
| 结项管理 | 结项申请、验收报告、财务审批、已结项只读 |
| 查询报表 | 年度/月度趋势、明细、Excel导出下载 |
| OCR/LLM | PaddleOCR识别、规则抽取、LLM兜底、识别日志 |
| 系统管理 | 用户、字典、审批流配置、日志、OCR状态、LLM配置 |

## 5. 业务架构

### 5.1 立项主流程

```text
商务上传合同 -> 文档解析/OCR -> 草稿保存 -> 盖章合同差异 -> 人工确认 -> 提交审核
        -> 管理员审批 -> 已立项 -> 管理员确认开始 -> 进行中
```

### 5.2 财务主流程

```text
财务选择项目 -> 发票识别 -> 开票登记 -> 回款凭证识别 -> 项目级回款登记 -> 台账更新
```

### 5.3 结项主流程

```text
项目经理提交结项 -> 回款未完成提示 -> 财务查看结项详情 -> 财务审批 -> 已结项只读
```

## 6. 数据流设计

### 6.1 合同识别数据流

1. 前端上传 Word/.doc/PDF/图片合同。
2. 后端保存文件到 uploads。
3. Word/.doc 进入文档解析，PDF/图片进入 PaddleOCR。
4. 后端规则抽取项目名称、金额、甲乙丙方、日期、合同编号等字段。
5. 若配置启用 LLM 且关键字段缺失，则调用当前启用模型兜底。
6. 草稿或差异数据写入数据库。
7. 识别日志写入 `ocr_recognition_log`。

### 6.2 财务数据流

1. 发票文件上传后 OCR 识别税务字段。
2. 后端补齐不含税金额、税率、税额、价税合计。
3. 保存发票并更新项目台账。
4. 回款凭证上传后识别本次回款金额。
5. 保存回款时校验项目累计回款不超过累计开票价税合计。
6. 台账、报表和导出读取真实业务数据。

## 7. 接口规范

### 7.1 基础规范

- 所有业务接口统一前缀：`/api/v1/`。
- JSON 接口使用 `application/json`。
- 文件型业务接口使用 `multipart/form-data`。
- 登录后通过 `Authorization: Bearer <token>` 鉴权。
- 统一响应格式：

```json
{
  "code": 200,
  "message": "success",
  "data": {}
}
```

### 7.2 主要接口

| 模块 | 接口示例 | 说明 |
|---|---|---|
| 用户 | `/api/v1/user/login`、`/api/v1/user/list`、`/api/v1/user/create` | 登录和用户管理 |
| 项目 | `/api/v1/project/list`、`/api/v1/project/word/parse-draft`、`/api/v1/project/stamped-contract/parse` | 项目查询、合同解析、差异生成 |
| 审批 | `/api/v1/approval/task/list`、`/api/v1/approval/process` | 待办和审批处理 |
| 发票 | `/api/v1/invoice/create`、`/api/v1/invoice/list` | 发票登记和查询 |
| 回款 | `/api/v1/payment/create`、`/api/v1/payment/list` | 项目级回款登记和查询 |
| 财务 | `/api/v1/finance/summary` | 台账汇总 |
| 报表 | `/api/v1/statistics/report` | 年度/月度统计 |
| 导出 | `/api/v1/export/batch`、`/api/v1/export/download` | 运行时导出任务和下载 |
| 系统 | `/api/v1/system/config/llm`、`/api/v1/system/ocr-health` | LLM配置和OCR状态 |

## 8. 部署架构

```text
frontend  Nginx       :80
backend   FastAPI     :8000
db        MySQL 8.0   :3306
paddleocr OCR Service :8001
```

后端启动时执行数据库迁移和兼容补丁，并初始化默认用户、角色、字典和审批模板。

## 9. 安全与一致性设计

- 密码使用 BCrypt 哈希。
- JWT 负责接口鉴权。
- 后端角色依赖负责权限控制。
- 管理员为超级角色。
- API Key 等敏感配置不在前端和日志中明文回显。
- 金额字段使用 `DECIMAL`。
- 发票号码唯一约束。
- 开票、回款、删除等金额敏感操作使用事务校验。
- 不实现软删除，关键操作写审计日志。

## 10. 可扩展设计

- OCR 与后端解耦，后续可替换识别服务。
- LLM 配置保存在数据库，支持多个模型和即时切换。
- 导出任务保持运行时队列，后续可替换为 Redis/Celery。
- 审批模板、审批节点和审批人配置独立建模，后续可扩展更多审批类型。
