# 智能项目管理系统 - 概要设计说明书

## 1. 引言

### 1.1 编写目的
本文档是智能项目管理系统的概要设计说明书，旨在描述系统的整体架构、模块划分、接口设计和数据流设计，为详细设计和编码实现提供指导。

### 1.2 项目背景
企业项目管理中，项目立项、开票回款、项目结项是三个核心环节，目前依赖线下文档流转和手工记录，存在信息分散、数据不透明、查询困难等问题。本系统旨在构建一套Web端智能项目管理系统，实现全生命周期数字化管理。

### 1.3 术语定义
| 术语 | 说明 |
|------|------|
| OCR | 光学字符识别 |
| NLP | 自然语言处理 |
| PaddleOCR | 百度开源OCR工具 |
| RBAC | 基于角色的访问控制 |
| FastAPI | Python高性能异步框架 |

---

## 2. 系统架构设计

### 2.1 总体架构

```
┌─────────────────────────────────────────────────────────────────┐
│                        用户层 (Browser)                         │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │              Vue 3 + Element Plus 前端应用               │   │
│  └─────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
                              │ HTTP/HTTPS
                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                        服务层 (Server)                          │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │              FastAPI 后端应用 (Python 3.11)              │   │
│  │  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐      │   │
│  │  │ 用户模块 │ │ 项目模块 │ │ 开票模块 │ │ 结项模块 │      │   │
│  │  └─────────┘ └─────────┘ └─────────┘ └─────────┘      │   │
│  │  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐      │   │
│  │  │ 审批模块 │ │ OCR模块  │ │ 导出模块 │ │ 统计模块 │      │   │
│  │  └─────────┘ └─────────┘ └─────────┘ └─────────┘      │   │
│  └─────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
                              │
          ┌───────────────────┼───────────────────┐
          ▼                   ▼                   ▼
┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│   MySQL     │     │ PaddleOCR   │     │  本地文件    │
│   数据库    │     │ Docker容器  │     │   存储      │
└─────────────┘     └─────────────┘     └─────────────┘
```

### 2.2 技术架构

| 层级 | 技术选型 | 说明 |
|------|----------|------|
| 前端 | Vue 3 + Element Plus | 响应式UI，组件化开发 |
| 后端 | Python 3.11 + FastAPI | 高性能异步框架 |
| 数据库 | MySQL 8.0 | 关系型数据存储 |
| OCR服务 | PaddleOCR + 百度OCR(兜底) | 本地部署，数据安全 |
| 文件存储 | 本地存储 | 合同、发票等文件 |
| 部署 | Docker + Docker Compose | 容器化部署 |

### 2.3 部署架构

```
┌─────────────────────────────────────────────────────────────────┐
│                     Docker Compose 部署                          │
│                                                                 │
│  ┌─────────────┐    ┌─────────────┐    ┌─────────────────────┐ │
│  │   Nginx     │    │   FastAPI   │    │    PaddleOCR        │ │
│  │  (前端)     │───▶│   (后端)    │───▶│   (OCR服务)         │ │
│  │   :80       │    │   :8000     │    │    :8001            │ │
│  └─────────────┘    └─────────────┘    └─────────────────────┘ │
│                            │                                    │
│                            ▼                                    │
│                    ┌─────────────┐                              │
│                    │    MySQL    │                              │
│                    │   :3306     │                              │
│                    └─────────────┘                              │
└─────────────────────────────────────────────────────────────────┘
```

---

## 3. 模块划分

### 3.1 模块总览

```
智能项目管理系统
├── 用户管理模块 (sys)
│   ├── 用户管理
│   ├── 角色管理
│   ├── 数据字典
│   └── 操作日志
│
├── 工作台模块 (dashboard)
│   ├── 待办事项
│   ├── 数据概览
│   └── 快捷入口
│
├── 立项管理模块 (project)
│   ├── 立项前登记
│   ├── 立项信息登记
│   ├── 合同校验
│   ├── 差异确认
│   ├── 立项审核
│   └── 项目列表
│
├── 开票管理模块 (invoice)
│   ├── 开票登记
│   ├── 发票OCR识别
│   └── 开票记录
│
├── 回款管理模块 (payment)
│   ├── 回款登记
│   ├── 应收款计算
│   └── 回款记录
│
├── 结项管理模块 (close)
│   ├── 结项申请
│   ├── 结项审核
│   ├── 结项撤回
│   └── 尾款状态
│
├── 查询汇总模块 (statistics)
│   ├── 数据查询
│   ├── 数据可视化
│   └── 报表导出
│
├── 审批流模块 (approval)
│   ├── 审批流配置
│   ├── 审批实例
│   ├── 审批任务
│   ├── 审批处理
│   └── 审批记录
│
└── OCR服务模块 (ocr)
    ├── PaddleOCR识别
    ├── 百度OCR兜底
    └── 识别日志
```

### 3.2 模块职责

| 模块 | 职责 | 主要功能 |
|------|------|----------|
| 用户管理 | 系统基础管理 | 用户CRUD、角色权限、数据字典、操作日志 |
| 工作台 | 个人工作入口 | 待办展示、数据统计、快捷操作 |
| 立项管理 | 项目立项全流程 | 合同上传、文档解析/OCR识别、信息登记、差异确认、审核审批 |
| 开票管理 | 开票登记管理 | 开票登记、发票OCR、开票记录 |
| 回款管理 | 回款登记管理 | 回款登记、应收款计算、回款记录 |
| 结项管理 | 项目结项流程 | 结项申请、审核审批、结项撤回 |
| 查询汇总 | 数据统计分析 | 多维查询、图表展示、报表导出 |
| 审批流 | 审批流程管理 | 审批配置、审批实例、审批任务、审批处理、审批记录 |
| OCR服务 | 智能识别服务 | 合同识别、发票识别、兜底处理 |

---

## 4. 接口设计

### 4.1 接口规范

**请求格式**：
```
POST /api/v1/{module}/{action}
Content-Type: application/json
Authorization: Bearer {token}
```

**文件上传格式**：
- 涉及合同、发票、验收报告等附件的业务接口直接使用 `multipart/form-data`
- 表单字段与文件字段在同一次请求中提交，不设计单独的通用文件预上传接口
- `.doc/.docx` 合同走文档解析 + NLP 信息提取；PDF/图片走 OCR + NLP 信息提取

**响应格式**：
```json
{
  "code": 200,
  "message": "success",
  "data": {}
}
```

**错误码**：
| 错误码 | 说明 |
|--------|------|
| 200 | 成功 |
| 400 | 请求参数错误 |
| 401 | 未授权 |
| 403 | 权限不足 |
| 404 | 资源不存在 |
| 500 | 服务器内部错误 |

### 4.2 接口列表

#### 用户管理接口
| 接口 | 方法 | 说明 |
|------|------|------|
| /api/v1/user/login | POST | 用户登录 |
| /api/v1/user/logout | POST | 用户退出 |
| /api/v1/user/list | GET | 用户列表 |
| /api/v1/user/create | POST | 创建用户 |
| /api/v1/user/update | PUT | 更新用户 |
| /api/v1/user/delete | DELETE | 删除用户 |

#### 立项管理接口
| 接口 | 方法 | 说明 |
|------|------|------|
| /api/v1/project/list | GET | 项目列表 |
| /api/v1/project/create | POST | 创建项目，`multipart/form-data` 支持Word合同附件 |
| /api/v1/project/update | PUT | 更新项目，`multipart/form-data` 支持补充PDF合同 |
| /api/v1/project/delete | DELETE | 删除项目 |
| /api/v1/project/detail | GET | 项目详情 |
| /api/v1/project/submit | POST | 提交审核 |
| /api/v1/project/approve | POST | 审核项目 |
| /api/v1/project/start | POST | 管理员确认项目开始，状态由已立项变为进行中 |
| /api/v1/project/batch-approve | POST | 批量审核 |
| /api/v1/project/terminate | POST | 终止项目 |

#### 开票管理接口
| 接口 | 方法 | 说明 |
|------|------|------|
| /api/v1/invoice/list | GET | 开票列表 |
| /api/v1/invoice/create | POST | 开票登记，`multipart/form-data` 支持发票附件 |
| /api/v1/invoice/detail | GET | 开票详情 |

#### 回款管理接口
| 接口 | 方法 | 说明 |
|------|------|------|
| /api/v1/payment/list | GET | 回款列表 |
| /api/v1/payment/create | POST | 回款登记，必须绑定单张发票 |
| /api/v1/payment/detail | GET | 回款详情 |

#### 结项管理接口
| 接口 | 方法 | 说明 |
|------|------|------|
| /api/v1/close/list | GET | 结项列表 |
| /api/v1/close/apply | POST | 结项申请，`multipart/form-data` 支持验收报告 |
| /api/v1/close/approve | POST | 财务结项审核，通过后项目变为已结项 |
| /api/v1/close/batch-approve | POST | 批量审核 |
| /api/v1/close/withdraw | POST | 撤回结项 |

#### OCR服务接口
| 接口 | 方法 | 说明 |
|------|------|------|
| /api/v1/ocr/contract | POST | 合同识别 |
| /api/v1/ocr/invoice | POST | 发票识别 |

#### 统计导出接口
| 接口 | 方法 | 说明 |
|------|------|------|
| /api/v1/statistics/overview | GET | 数据概览 |
| /api/v1/statistics/project | GET | 项目统计 |
| /api/v1/statistics/finance | GET | 财务统计 |
| /api/v1/export/excel | POST | 导出Excel |
| /api/v1/export/pdf | POST | 导出PDF |
| /api/v1/export/batch | POST | 批量导出 |
| /api/v1/export/status | GET | 查询运行时导出任务状态 |

说明：批量导出任务不落库，不设计导出任务表和导出历史表。`task_id` 仅作为运行时任务队列中的临时标识，服务重启后历史任务状态不保证保留。

#### 审批流接口
| 接口 | 方法 | 说明 |
|------|------|------|
| /api/v1/approval/template/create | POST | 创建审批模板和节点配置 |
| /api/v1/approval/instance/list | GET | 审批实例列表 |
| /api/v1/approval/task/list | GET | 当前用户审批任务列表 |
| /api/v1/approval/process | POST | 处理审批任务 |

---

## 5. 数据流设计

### 5.1 立项流程数据流

```
┌────────┐    ┌────────┐    ┌────────┐    ┌────────┐    ┌────────┐
│ 上传   │    │ 解析/  │    │ 信息   │    │ 合同   │    │ 提交   │
│ 合同   │───▶│ OCR识别 │───▶│ 登记   │───▶│ 校验   │───▶│ 审批   │
│ 合同   │    │        │    │        │    │        │    │        │
└────────┘    └────────┘    └────────┘    └────────┘    └────────┘
                   │              │              │              │
                   ▼              ▼              ▼              ▼
              ┌────────┐    ┌────────┐    ┌────────┐    ┌────────┐
              │提取信息│    │填充表单│    │差异清单│    │审批实例│
              └────────┘    └────────┘    └────────┘    └────────┘
```

说明：Word 合同通过文档解析获取文本后进行 NLP 提取；PDF/图片合同通过 OCR 获取文本后进行 NLP 提取。合同校验产生的差异结果、人工采用值、确认人、确认时间和备注需要持久化。

### 5.2 开票流程数据流

```
┌────────┐    ┌────────┐    ┌────────┐    ┌────────┐
│ 选择   │    │ 上传   │    │ OCR    │    │ 开票   │
│ 项目   │───▶│ 发票   │───▶│ 识别   │───▶│ 登记   │
└────────┘    └────────┘    └────────┘    └────────┘
                   │              │              │
                   ▼              ▼              ▼
              ┌────────┐    ┌────────┐    ┌────────┐
              │发票文件│    │提取信息│    │开票记录│
              └────────┘    └────────┘    └────────┘
```

### 5.3 结项流程数据流

```
┌────────┐    ┌────────┐    ┌────────┐    ┌────────┐
│ 选择   │    │ 记录   │    │ 提交   │    │ 财务   │
│ 项目   │───▶│ 结项时间│───▶│ 申请   │───▶│ 审批   │
│        │    │ 报告   │    │        │    │        │
└────────┘    └────────┘    └────────┘    └────────┘
                   │              │              │
                   ▼              ▼              ▼
              ┌────────┐    ┌────────┐    ┌────────┐
              │报告文件│    │申请记录│    │变为已结项│
              └────────┘    └────────┘    └────────┘
```

说明：项目经理记录结项时间并提交结项审批；财务审批通过后项目状态变更为「已结项」，项目只读且不可再开票。管理员仅保留结项撤回能力。

---

## 6. 安全设计

### 6.1 认证授权
- **认证方式**：JWT Token
- **授权模型**：RBAC（基于角色的访问控制）
- **角色模型**：每个用户仅允许绑定一个角色，通过 `sys_user.role` 标识
- **密码存储**：BCrypt加密

### 6.2 数据安全
- **传输加密**：HTTPS
- **敏感数据**：采用数据库透明加密（TDE）或等效数据库/磁盘透明加密能力，业务金额字段仍以DECIMAL存储以支持统计、校验和排序
- **文件安全**：文件类型校验、大小限制

### 6.3 操作安全
- **操作日志**：记录所有关键操作
- **IP限制**：支持IP白名单
- **超时控制**：会话超时自动退出

---

## 7. 性能设计

### 7.1 性能指标
| 指标 | 要求 |
|------|------|
| 页面加载时间 | ≤2秒 |
| OCR识别响应 | ≤2秒(GPU)/≤5秒(CPU) |
| 并发用户数 | ≥100 |
| 数据库查询 | ≤1秒 |

### 7.2 性能优化
- **前端优化**：代码分割、懒加载、CDN加速
- **后端优化**：数据库索引、缓存机制、异步处理
- **OCR优化**：队列机制、批量处理

---

## 8. 可靠性设计

### 8.1 高可用
- **服务冗余**：支持多实例部署
- **故障转移**：自动故障检测和转移
- **数据备份**：每日自动备份

### 8.2 容错设计
- **OCR兜底**：PaddleOCR失败时自动切换百度OCR
- **异常处理**：统一异常处理和错误提示
- **重试机制**：网络异常自动重试

---

## 9. 可维护性设计

### 9.1 代码规范
- **命名规范**：统一命名规范
- **注释规范**：关键代码添加注释
- **代码审查**：定期代码审查

### 9.2 日志管理
- **日志级别**：DEBUG/INFO/WARN/ERROR
- **日志格式**：统一日志格式
- **日志存储**：按日期归档

---

## 10. 附录

### 10.1 参考文档
- 需求说明书
- 数据库设计说明书
- 详细设计说明书
