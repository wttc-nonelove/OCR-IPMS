# 智能项目管理系统开发与部署报告

## 一、报告说明

本报告根据当前项目完整代码、设计文档、操作手册、Docker 配置，以及 GitHub 仓库 `wttc-nonelove/OCR-IPMS` 在 `develop-cc` 分支的提交日志整理形成。报告内容包括项目背景、技术架构、主要功能、开发历程、部署操作教程、验证方式和常见问题处理。

当前报告对应代码状态：

- 本地项目路径：`F:\课设\OCR-NLP`
- GitHub 仓库：`https://github.com/wttc-nonelove/OCR-IPMS`
- 当前分支：`develop-cc`
- 当前最新提交：`b65f0d2 feat: 优化报表年月查询与界面交互`
- 当前部署方式：Docker Compose 一键部署

## 二、项目概况

智能项目管理系统面向项目立项、合同识别、开票回款、结项审批和统计报表等业务场景。系统通过 OCR 与文档解析能力辅助录入合同、发票和回款凭证信息，通过审批流保证立项、开票和结项过程可追踪，通过财务汇总和报表导出支持项目经营分析。

系统设计目标包括：

1. 支持商务、管理员、财务、项目经理四类角色按权限使用系统。
2. 支持 Word 合同解析、PDF/图片 OCR 识别和合同差异确认。
3. 支持项目从草稿、待审核、已立项、进行中到已结项的状态流转。
4. 支持发票登记、回款登记、发票税务字段识别、金额校验和删除重登。
5. 支持审批实例、审批任务和审批记录，保证审批过程可审计。
6. 支持查询报表、年度/月度趋势、Excel 导出和浏览器下载。
7. 支持 Docker Compose 一键部署前端、后端、MySQL 和 PaddleOCR 服务。

## 三、系统技术架构

### 3.1 技术栈

| 层级 | 技术 |
|------|------|
| 前端 | Vue 3、Element Plus、Pinia、Vue Router、Vite |
| 后端 | FastAPI、SQLAlchemy、Pydantic、JWT、BCrypt |
| 数据库 | MySQL 8.0，开发测试兼容 SQLite |
| OCR 服务 | 独立 PaddleOCR 服务，HTTP 接口调用 |
| 文档解析 | Word 文档解析、PDF 转图片识别、图片 OCR |
| 文件存储 | 本地 `uploads` 目录挂载 |
| 部署 | Docker Compose，前端 Nginx，后端 Uvicorn |
| 导出 | openpyxl 生成 `.xlsx`，运行时任务队列维护 |

### 3.2 服务组成

Docker Compose 启动后包含四个核心服务：

| 服务 | 说明 | 默认端口 |
|------|------|----------|
| `frontend` | Vue 前端构建产物，由 Nginx 提供访问 | `80` |
| `backend` | FastAPI 后端业务服务 | `8000` |
| `db` | MySQL 8.0 数据库 | `3306` |
| `paddleocr` | OCR 识别服务 | `8001` 映射到容器内 `8000` |

### 3.3 后端模块

后端按业务模块拆分为：

- `auth.py`：登录、JWT 鉴权、用户信息。
- `project.py`：立项草稿、合同解析、差异确认、项目状态流转。
- `invoice.py`：发票 OCR、发票登记、发票删除、金额校验。
- `payment.py`：回款凭证识别、回款登记、回款删除。
- `approval.py`：审批实例、审批任务、审批记录。
- `close.py`：项目结项申请、结项审批。
- `finance.py`：财务汇总、开票回款进度和未回款标注。
- `statistics.py`：工作台统计、年度/月度报表。
- `export.py`：导出任务创建、状态查询和文件下载。
- `system.py`：字典、日志、OCR 健康状态、系统管理。

### 3.4 前端页面

前端页面包括：

- 登录页：带云层和视觉动效的登录界面。
- 工作台：按角色展示指标、待办和业务入口。
- 立项管理：商务创建草稿、上传合同、确认差异、提交审核；管理员审核和查看详情。
- 开票回款：使用页内 Tabs 分离开票登记和回款登记，支持发票记录和回款记录切换。
- 结项管理：项目经理提交结项，财务查看结项内容并审批。
- 查询报表：支持按年、按月查询趋势和明细，支持导出下载。
- 系统管理：用户管理、字典、审批模板、审计日志、OCR 状态。

## 四、核心业务流程

### 4.1 立项流程

1. 商务登录后进入立项管理。
2. 上传 Word 合同，系统解析项目名称、甲方、乙方、合同金额、合同编号、签订日期和项目类型。
3. 系统自动保存草稿，刷新页面后可恢复未提交内容。
4. 商务上传盖章 PDF 或图片合同，系统通过 PaddleOCR 识别并生成差异。
5. 商务确认差异，人工采用值写回项目。
6. 商务提交立项审核，系统创建审批实例和审批任务。
7. 管理员查看立项详情，填写备注后通过或驳回。
8. 管理员审核通过后项目变为已立项，确认开始后变为进行中。

### 4.2 开票与回款流程

1. 财务选择进行中项目。
2. 上传发票图片或 PDF，系统识别发票号码、开票日期、购方、销方、不含税金额、税率、税额和价税合计。
3. 系统保存发票时按不含税金额校验合同额度，价税合计用于票面金额和回款上限。
4. 财务上传回款凭证，系统识别回款金额、回款日期、付款方、收款方和关联发票号。
5. 回款必须绑定单张发票，累计回款金额不能超过该发票价税合计。
6. 已保存的发票和回款支持删除重登；如果发票已有回款，删除发票返回“该发票有回款不允许删除”。

### 4.3 结项流程

1. 项目经理选择已立项或进行中的项目。
2. 填写结项时间、结项说明，并上传验收报告或附件。
3. 如果项目回款未完成，系统给出未回款提示，但允许继续提交结项审批。
4. 财务查看项目经理提交的结项内容、项目详情和财务明细。
5. 财务审批通过后，项目状态变为已结项。
6. 已结项项目只读，不允许继续开票。

### 4.4 查询报表与导出

1. 查询报表页支持按年查询和按月查询。
2. 年度模式展示 1 到 12 月开票、回款趋势。
3. 月度模式展示当月每日开票、回款趋势，并提供当月发票记录和回款记录 Tabs。
4. 导出任务不落库，只保存在运行时队列。
5. 导出完成后通过 `/api/v1/export/download?task_id=` 由浏览器下载 `.xlsx` 文件。

## 五、开发历程

项目开发经历了“原型与基础框架、核心业务闭环、OCR 与差异修复、财务增强、系统管理与报表优化”等阶段。提交日志反映出项目从 MVP 逐步迭代为较完整的业务系统。

### 5.1 提交历程总览

| 提交 | 日期 | 主题 | 主要内容 |
|------|------|------|----------|
| `79f3036` | 2026-06-08 | initial commit | 建立 FastAPI 后端、Vue3 前端、PaddleOCR 服务、Docker Compose、设计文档和 HTML 原型 |
| `8af783a` | 2026-06-08 | 完善立项解析草稿与审批流联动 | Word 合同解析、后端草稿、甲乙方字段、PDF 合同差异、真实审批待办和前端错误提示 |
| `1419ae1` | 2026-06-08 | 修复 OCR 解析差异与财务交互 | 修复合同金额解析、PDF 差异生成、管理员审批备注、发票与回款凭证识别、结项报告上传和 OCR 状态 |
| `1b97d68` | 2026-06-08 | 修复盖章合同 PDF OCR | 增加 PyMuPDF，PDF 先转图片再 OCR；修复 `Failed importing fitz`；新增项目删除和编号展示区分 |
| `5c5eba8` | 2026-06-08 | 项目全面优化 | 安全加固、分页、Alembic 迁移、测试补充、代码拆分重构 |
| `3a2143f` | 2026-06-08 | 发票税额字段扩展 | 增加不含税金额、税率、税额字段；发票 OCR 提取税务字段；金额校验改为不含税金额 |
| `a160953` | 2026-06-09 | 完善财务税务字段与审批详情交互 | 增强税务字段抽取，修复开票回款项目联动，扩展项目和结项详情，允许管理员删除测试数据 |
| `ed45319` | 2026-06-09 | 功能增强 | 增加发票/回款删除、导出下载、未回款状态、PM 财务查看权限和报表导出下载 |
| `4a3f1bb` | 2026-06-09 | 用户管理完善 | 增加用户创建、编辑、启禁用、删除、修改密码接口和前端对话框 |
| `df0c5ab` | 2026-06-09 | 优化管理员用户管理 | 优化用户管理窗口、状态显示、新增用户体验 |
| `b65f0d2` | 2026-06-09 | 优化报表年月查询与界面交互 | 报表支持年度/月度双模式，导出支持年月过滤，报表 UI 明显改版 |

### 5.2 第一阶段：项目初始化

初始提交完成了系统骨架：

- 建立 `backend`、`frontend`、`paddleocr`、`prototype`、`uploads` 等目录。
- 后端实现 FastAPI 应用、认证、项目、发票、OCR、审批和统计模块。
- 前端使用 Vue 3、Element Plus、Pinia 和 Vue Router 搭建页面框架。
- Docker Compose 编排 MySQL、后端、前端和 OCR 服务。
- 保留需求说明书、概要设计、数据库设计、详细设计和 HTML 原型作为开发依据。

### 5.3 第二阶段：立项与审批闭环

该阶段重点解决商务立项的真实性和持久化问题：

- Word 合同上传后自动解析并创建或更新草稿。
- 草稿保存到数据库，刷新页面后不会丢失。
- 增加甲方、乙方字段，`customer` 兼容甲方。
- 项目类型允许从合同解析结果自动扩展字典。
- PDF/图片盖章合同走 OCR，并与登记值生成差异。
- 差异确认后保存登记值、识别值、人工采用值、确认人、确认时间和备注。
- 立项提交后创建审批实例和审批任务，管理员可通过或驳回。

### 5.4 第三阶段：OCR 与合同差异修复

在实际测试中，系统暴露出合同金额识别错误、PDF OCR 失败和差异显示不准确等问题。后续提交针对这些问题进行了修复：

- 合同金额提取改为优先识别金额标签后的阿拉伯金额，再处理中文大写金额。
- 避免错误提取年份、合同编号等非金额数字。
- PDF 文件不再直接交给 OCR，而是使用 PyMuPDF 先转图片再逐页识别。
- OCR 接口返回识别原文摘要、行数、置信度、未识别字段和解析状态。
- 差异生成时只对识别值非空的字段比较，无法识别的字段单独提示人工确认。

### 5.5 第四阶段：财务开票回款增强

财务模块经历了多轮修复和增强：

- 发票识别支持发票号码、开票日期、购方、销方、不含税金额、税率、税额和价税合计。
- 开票额度校验使用不含税金额，避免价税合计超过合同金额时误拦截。
- 回款金额校验使用关联发票价税合计，保证票面总额与回款逻辑一致。
- 回款必须绑定单张发票。
- 发票有回款时禁止删除发票。
- 已保存发票和回款支持删除后重新上传保存。
- 财务、管理员、项目经理都可以按权限查看发票回款详细情况。

### 5.6 第五阶段：系统管理与安全加固

项目从功能可用进一步向工程可维护性优化：

- 后端增加分页能力，项目、发票、回款、结项、日志和 OCR 日志均支持分页。
- 增加 Alembic 迁移脚本，启动时优先执行迁移。
- 移除硬编码安全配置，`SECRET_KEY`、数据库密码和 CORS 支持环境变量。
- 增加 pytest 测试，覆盖认证、项目、发票、分页、用户管理等模块。
- 用户管理支持新增、编辑、启用、禁用、删除和修改密码。
- 禁止管理员禁用或删除当前登录账号。

### 5.7 第六阶段：报表与 UI 优化

最后阶段聚焦报表和界面可见性：

- 查询报表由单一年度汇总升级为年度/月度双模式。
- 年度模式展示 12 个月趋势。
- 月度模式展示当月每日趋势和当月发票、回款明细。
- 导出任务支持 `mode`、`year`、`month`、`keyword` 参数。
- 导出文件以 `.xlsx` 形式通过浏览器下载。
- 登录页、全局 UI、报表页视觉结构进行了明显优化。

## 六、部署操作教程

### 6.1 部署前准备

部署机器需要安装：

1. Docker Desktop 或 Docker Engine。
2. Docker Compose。
3. Git。
4. 可用端口：`80`、`8000`、`8001`、`3306`。

如果本机已经有 MySQL 或 Nginx 占用端口，需要先停止对应服务，或修改 `docker-compose.yml` 的端口映射。

### 6.2 获取代码

```powershell
git clone https://github.com/wttc-nonelove/OCR-IPMS.git
cd OCR-IPMS
git checkout develop-cc
```

如果已经有本地仓库：

```powershell
cd F:\课设\OCR-NLP
git checkout develop-cc
git pull origin develop-cc
```

### 6.3 配置环境变量

项目根目录存在 `.env.docker` 模板。部署时可复制为 `.env`：

```powershell
Copy-Item .env.docker .env
```

关键配置项包括：

| 配置 | 说明 |
|------|------|
| `MYSQL_ROOT_PASSWORD` | MySQL root 密码 |
| `MYSQL_DATABASE` | 业务数据库名，默认 `project_mgmt` |
| `MYSQL_USER` | 业务数据库用户 |
| `MYSQL_PASSWORD` | 业务数据库密码 |
| `SECRET_KEY` | JWT 签名密钥，生产环境必须修改 |

如果不创建 `.env`，Compose 会使用 `docker-compose.yml` 中的默认值，适合本地演示，不适合生产环境。

### 6.4 一键启动

在项目根目录执行：

```powershell
docker compose up -d --build
```

首次启动会构建前端、后端和 OCR 镜像，并启动 MySQL。MySQL 初始化需要一定时间，后端会等待数据库健康检查。

### 6.5 查看服务状态

```powershell
docker compose ps
```

正常情况下应看到：

- `backend` 为 `Up`
- `frontend` 为 `Up`
- `db` 为 `Up` 且 `healthy`
- `paddleocr` 为 `Up` 且 `healthy`

当前本地验证状态显示四个服务均已运行，其中 MySQL 和 PaddleOCR 均为 healthy。

### 6.6 访问系统

部署成功后访问：

| 地址 | 用途 |
|------|------|
| `http://localhost` | 前端系统 |
| `http://localhost:8000/health` | 后端健康检查 |
| `http://localhost:8000/docs` | FastAPI 接口文档 |
| `http://localhost:8001/health` | PaddleOCR 健康检查 |

### 6.7 默认账号

系统首次启动且数据库为空时，会自动初始化默认用户：

| 角色 | 用户名 | 密码 | 主要功能 |
|------|--------|------|----------|
| 管理员 | `admin` | `123456` | 立项审核、项目开始、系统管理、报表、删除测试数据 |
| 商务 | `business01` | `123456` | 立项登记、合同解析、差异确认、提交审核 |
| 财务 | `finance01` | `123456` | 发票登记、回款登记、结项审批、报表导出 |
| 项目经理 | `pm01` | `123456` | 查看项目、查看财务明细、提交结项 |

### 6.8 推荐演示流程

1. 商务使用 `business01 / 123456` 登录。
2. 进入立项管理，上传 Word 合同，确认解析结果。
3. 保存草稿并上传盖章合同 PDF 或图片。
4. 确认合同差异并提交审核。
5. 管理员使用 `admin / 123456` 登录。
6. 查看商务提交的立项详情，填写备注并审核通过。
7. 管理员确认项目开始，项目进入进行中。
8. 财务使用 `finance01 / 123456` 登录。
9. 进入开票回款，上传发票图片，确认识别字段并保存。
10. 上传回款凭证，绑定发票并保存回款。
11. 项目经理使用 `pm01 / 123456` 登录。
12. 查看项目、合同差异和财务明细，提交结项申请。
13. 财务审批结项，项目变为已结项。
14. 进入查询报表，按年或按月查看趋势并导出 Excel。

## 七、常用维护命令

### 7.1 查看日志

```powershell
docker compose logs -f backend
docker compose logs -f frontend
docker compose logs -f db
docker compose logs -f paddleocr
```

### 7.2 重启服务

```powershell
docker compose restart
```

单独重启后端：

```powershell
docker compose restart backend
```

### 7.3 更新代码并重新部署

```powershell
git pull origin develop-cc
docker compose up -d --build backend frontend paddleocr
```

如果数据库模型有迁移，后端启动时会自动执行 `alembic upgrade head`。迁移失败时会回退到 `create_all` 以兼容开发环境。

### 7.4 停止系统

```powershell
docker compose down
```

### 7.5 清空数据库并重建

该操作会删除 MySQL 数据卷，适用于演示数据需要重置的情况：

```powershell
docker compose down -v
docker compose up -d --build
```

### 7.6 前端看不到最新界面

如果代码已经更新但浏览器仍显示旧界面，可按以下方式处理：

1. 使用 `Ctrl + F5` 强制刷新浏览器缓存。
2. 确认前端镜像已重新构建：

```powershell
docker compose up -d --build frontend
```

3. 查看前端容器日志：

```powershell
docker compose logs frontend
```

### 7.7 OCR 服务异常

检查 PaddleOCR 健康状态：

```powershell
curl http://localhost:8001/health
docker compose logs -f paddleocr
```

如果 PDF 识别失败，需要确认 PaddleOCR 容器内包含 PyMuPDF，并确认 PDF 转图片流程正常。当前项目已经在后续提交中修复 `Failed importing fitz` 问题。

## 八、测试与验证

### 8.1 后端语法检查

```powershell
python -m compileall backend\app paddleocr
```

### 8.2 前端构建

```powershell
cd frontend
npm.cmd run build
```

### 8.3 后端测试

```powershell
cd backend
pytest -q
```

### 8.4 Docker 验证

```powershell
docker compose up -d --build
docker compose ps
curl http://localhost:8000/health
curl http://localhost:8001/health
```

### 8.5 报表导出验证

1. 登录系统。
2. 进入查询报表。
3. 选择按年查询，查看 1 到 12 月趋势。
4. 切换按月查询，查看当月每日趋势。
5. 勾选项目、发票或回款导出类型。
6. 发起导出任务。
7. 任务完成后点击下载，浏览器下载 `.xlsx` 文件。

## 九、当前实现边界

1. 导出任务仅保存在运行时队列，服务重启后未完成导出任务视为失败，需要重新发起。
2. OCR 识别基于 PaddleOCR 结果和后端规则抽取，低置信或字段冲突时仍需要人工确认。
3. 不实现站内信、短信、邮件、自动催办和自动转交。
4. 不使用软删除，删除操作按业务规则硬删除或受外键限制。
5. 数据库透明加密依赖部署环境提供，业务代码不做字段级金额加密。
6. 当前默认只初始化一个项目经理账号，但项目经理可查看所有项目。

## 十、总结

智能项目管理系统从初始 MVP 逐步迭代为支持多角色、多业务闭环、OCR 辅助识别、审批流、财务管理、统计报表和 Docker 部署的一体化系统。项目开发过程中，围绕真实业务测试反馈持续修复了合同解析、PDF OCR、差异确认、发票税务字段、回款金额校验、数据刷新、跨角色详情查看、用户管理和报表查询等问题。

截至 `develop-cc` 分支最新提交 `b65f0d2`，系统已经具备完整演示能力：可通过 Docker Compose 启动，使用默认账号完成商务立项、管理员审核、财务开票回款、项目经理结项、财务审批和报表导出等核心流程。

