# 智能项目管理系统开发与部署报告（最终版）

## 一、报告说明

本报告根据当前最终版项目代码、设计文档、操作手册、Docker 配置和提交历史整理形成。报告内容包括项目概况、架构、核心业务流程、开发历程、部署教程、验证方式和最终边界。

当前项目状态：

- 本地路径：`F:\课设\OCR-NLP`
- 当前分支：`develop-do`
- 当前最新提交：`db3e932 fix: 修复OCR日志raw_result超大JSON撑爆MySQL列导致500错误`
- 部署方式：Docker Compose
- 前端入口：`http://localhost`
- 后端健康检查：`http://localhost:8000/health`
- OCR 健康检查：`http://localhost:8001/health`

## 二、项目概况

智能项目管理系统面向企业项目立项、合同识别、开票回款、结项审批和统计报表场景。系统通过 Word 文档解析、PaddleOCR、规则抽取和可选 LLM 兜底解析辅助录入合同、发票和回款凭证信息，通过审批流保证业务过程可追踪，通过台账和报表支持项目经营分析。

最终版主要能力：

1. 支持商务、管理员、财务、项目经理四类角色。
2. 管理员为超级角色，可处理其他角色任务。
3. 支持 Word/.doc 合同解析、PDF/图片 OCR、弱标识合同 LLM 兜底。
4. 支持合同编号为空。
5. 支持合同差异生成、人工采用和写回项目。
6. 支持发票税务字段识别和开票额度校验。
7. 支持项目级回款登记，不强制关联具体发票。
8. 支持项目台账、年度/月度报表和 Excel 下载。
9. 支持多模型 LLM 配置、审计日志、OCR 日志和 PaddleOCR 状态查看。
10. 支持 Docker Compose 一键部署。

## 三、技术架构

| 层级 | 技术 |
|---|---|
| 前端 | Vue 3、TypeScript、Element Plus、Pinia、Vue Router、Vite |
| 后端 | FastAPI、SQLAlchemy、Pydantic、JWT、BCrypt |
| 数据库 | MySQL 8.0 |
| OCR | PaddleOCR 独立服务 |
| LLM | OpenAI 兼容接口，数据库持久化多模型配置 |
| 文件 | 本地 uploads 挂载 |
| 导出 | openpyxl 生成 `.xlsx`，运行时任务队列 |
| 部署 | Docker Compose，前端 Nginx，后端 Uvicorn |

Docker Compose 服务：

| 服务 | 说明 | 端口 |
|---|---|---|
| frontend | Vue 构建产物，Nginx 提供访问 | 80 |
| backend | FastAPI 后端 | 8000 |
| db | MySQL 8.0 | 3306 |
| paddleocr | OCR 服务 | 8001 映射容器 8000 |

## 四、核心业务流程

### 4.1 立项流程

1. 商务上传 Word/.doc 合同。
2. 系统解析合同文本并回填草稿。
3. 弱标识合同可通过 LLM 兜底解析。
4. 商务上传盖章 PDF/图片合同。
5. 系统 OCR 后生成合同差异。
6. 商务确认差异，人工采用值写回项目。
7. 商务提交审核。
8. 管理员查看详情并通过或驳回。
9. 通过后项目为已立项，管理员确认开始后项目为进行中。

### 4.2 开票回款流程

1. 财务搜索并选择项目。
2. 上传发票，系统识别不含税金额、税率、税额、价税合计等字段。
3. 保存发票，按不含税金额校验合同额度。
4. 上传回款凭证，系统识别本次回款金额。
5. 保存回款，按项目级累计回款不超过累计开票价税合计校验。
6. 台账实时展示合同金额、累计开票、累计回款、未回款金额和进度。

### 4.3 结项流程

1. 项目经理选择已立项或进行中项目。
2. 上传验收报告，填写结项说明。
3. 回款未完成时系统提示未回款金额，但允许提交。
4. 财务查看结项内容、项目详情和财务明细。
5. 财务审批通过后项目变为已结项。
6. 已结项项目只读，不可继续开票。

### 4.4 报表导出流程

1. 选择年度或月度查询模式。
2. 输入关键字筛选项目。
3. 查看趋势图和明细。
4. 创建导出任务。
5. 轮询运行时任务状态。
6. 完成后浏览器下载 `.xlsx`。

## 五、开发历程

| 提交 | 日期 | 说明 |
|---|---|---|
| `79f3036` | 2026-06-08 | 初始化项目，建立后端、前端、PaddleOCR、Docker 和文档 |
| `8af783a` | 2026-06-08 | 完善立项解析草稿和审批流联动 |
| `1419ae1` | 2026-06-08 | 修复 OCR 解析差异与财务交互 |
| `1b97d68` | 2026-06-08 | 修复盖章合同 PDF OCR，增加 PDF 转图片 |
| `5c5eba8` | 2026-06-08 | 安全加固、分页、Alembic 迁移和测试 |
| `3a2143f` | 2026-06-08 | 发票税额字段扩展 |
| `a160953` | 2026-06-09 | 完善财务税务字段与审批详情交互 |
| `ed45319` | 2026-06-09 | 增强发票/回款删除、导出下载和 OCR 优化 |
| `4a3f1bb` | 2026-06-09 | 用户管理接口和前端对话框完善 |
| `b65f0d2` | 2026-06-09 | 报表年月查询与界面交互优化 |
| `7cb6ef7` | 2026-06-10 | 合同解析、财务回款、权限菜单和系统配置全面优化 |
| `dca99fc` | 2026-06-11 | 优化 Word/盖章合同解析，支持 WPS 格式和弱标识字段 |
| `d78519b` | 2026-06-11 | 优化 PDF 合同、发票和回款 OCR 规则 |
| `db3e932` | 2026-06-11 | 修复 OCR 日志 raw_result 过大导致数据库写入失败 |

## 六、部署教程

### 6.1 环境准备

需要安装：

- Docker Desktop 或 Docker Engine
- Docker Compose
- Git

确保端口 `80`、`8000`、`8001`、`3306` 未被占用。

### 6.2 启动系统

```powershell
cd F:\课设\OCR-NLP
docker compose up -d --build
```

仅重建前后端：

```powershell
docker compose up -d --build backend frontend
```

### 6.3 查看状态

```powershell
docker compose ps
docker compose logs -f backend
```

访问地址：

- 前端：`http://localhost`
- 后端健康检查：`http://localhost:8000/health`
- 后端接口文档：`http://localhost:8000/docs`
- OCR 健康检查：`http://localhost:8001/health`

### 6.4 默认账号

| 角色 | 用户名 | 密码 |
|---|---|---|
| 管理员 | admin | 123456 |
| 商务 | business01 | 123456 |
| 财务 | finance01 | 123456 |
| 项目经理 | pm01 | 123456 |

### 6.5 环境变量

可在 `.env` 或 `.env.docker` 中配置：

| 配置 | 说明 |
|---|---|
| MYSQL_ROOT_PASSWORD | MySQL root 密码 |
| MYSQL_DATABASE | 数据库名 |
| MYSQL_USER | 数据库用户 |
| MYSQL_PASSWORD | 数据库密码 |
| SECRET_KEY | JWT 签名密钥 |
| PADDLEOCR_URL | OCR 服务地址 |
| LLM_ENABLED | LLM 默认开关 |
| LLM_API_BASE_URL | LLM 默认 Base URL |
| LLM_API_KEY | LLM 默认 API Key |
| LLM_MODEL | LLM 默认模型 |

最终版也支持在系统管理页配置 LLM，多模型配置保存在数据库，优先级高于环境变量。

## 七、测试验证

常用验证命令：

```powershell
python -m compileall backend\app backend\alembic paddleocr
$env:PYTHONPATH='backend'; pytest -q
cd frontend
npm.cmd run build
docker compose up -d --build backend frontend
```

测试重点：

- 登录和角色权限。
- 商务合同解析、草稿恢复和差异确认。
- 管理员审批通过/驳回和项目启动。
- 发票税务字段识别和开票额度校验。
- 项目级回款金额校验。
- 项目经理结项和财务审批。
- 年度/月度报表。
- Excel 导出下载。
- LLM 多模型配置。
- OCR 日志和审计日志。

## 八、最终边界

- 不实现软删除。
- 不实现站内信、短信、邮件通知。
- 不实现导出任务落库。
- 不强制回款关联具体发票。
- LLM 仅做弱标识合同兜底，不替代规则明确识别结果。
- 生产环境需要修改默认密码、`SECRET_KEY` 和数据库密码，并配置 HTTPS、备份和密钥管理。

## 九、总结

截至最终版，系统已具备课程设计演示所需的完整能力：可通过 Docker Compose 启动，使用默认账号完成商务立项、合同解析、差异确认、管理员审批、财务开票回款、项目经理结项、财务审批、报表查询导出和系统配置管理等核心流程。
