# 智能项目管理系统 - 需求说明书

## 1. 项目概述

### 1.1 项目背景
企业项目管理中，**项目立项、开票回款、项目结项**是三个核心环节，目前依赖线下文档流转和手工记录，存在以下痛点：
- 信息分散，数据不透明
- 合同、发票等关键文档需人工录入，效率低且易出错
- 项目进度难以实时追踪
- 各部门协同困难，职责边界模糊
- 查询困难，数据汇总耗时

### 1.2 建设目标
构建 Web 端智能项目管理系统，实现：
- 项目从「**立项 → 开票回款 → 结项**」的**全生命周期数字化管理**
- 支持**多角色协同**（商务 / 财务 / 项目经理 / 管理员）
- **OCR/NLP** 合同与发票智能识别
- **数据可视化**查询与报表导出

### 1.3 技术架构

| 层级 | 技术选型 | 说明 |
|------|----------|------|
| 前端 | Vue 3 + Element Plus | 响应式UI，组件化开发 |
| 后端 | Python 3.11 + FastAPI | 高性能异步框架 |
| 数据库 | MySQL 8.0 | 关系型数据存储，Docker部署 |
| 文件存储 | 本地存储 | 合同、发票等文件 |
| 智能识别 | PaddleOCR（主）+ 百度OCR（兜底） | 本地部署，数据安全 |
| 部署 | Docker + Docker Compose | 容器化部署，一键启动 |

---

## 2. 用户角色与权限

### 2.1 角色定义

| 角色 | 职责模块 | 具体功能 |
|------|----------|----------|
| **管理员** | 系统管理 | 用户管理（新增/编辑/启禁）、角色管理（RBAC权限）、数据字典维护、立项审核 |
| **商务** | 立项管理 | 立项前信息登记、合同上传与OCR提取、合同校验比对、立项信息管理 |
| **财务** | 开票回款 | 开票登记与发票识别、回款记录管理、关联项目开票记录、应收款自动计算、查询汇总、结项审核 |
| **项目经理** | 结项管理 | 验收报告上传、结项登记、结项审批申请、项目状态跟踪 |

### 2.2 权限矩阵

| 功能模块 | 管理员 | 商务 | 财务 | 项目经理 |
|----------|--------|------|------|----------|
| 系统管理 | ✅ | ❌ | ❌ | ❌ |
| 数据字典 | ✅ | ❌ | ❌ | ❌ |
| 工作台 | ✅ | ✅ | ✅ | ✅ |
| 立项管理 | 审核 | 操作 | 查看 | 查看 |
| 开票管理 | 查看 | ❌ | 操作 | ❌ |
| 回款管理 | 查看 | ❌ | 操作 | ❌ |
| 查询汇总 | ✅ | 部分 | ✅ | 部分 |
| 结项管理 | 查看 | 查看 | 审核 | 申请 |

---

## 3. 核心业务流程

### 3.1 业务流程全景

```
商务 ──────────────── 财务 ──────────────── 项目经理
  │                    │                      │
  ▼                    ▼                      ▼
第一阶段：项目立项    第二阶段：开票回款     第三阶段：项目结项
  │                    │                      │
  ├─ 立项前登记        ├─ 开票登记            ├─ 上传验收报告
  ├─ 立项信息登记      ├─ 回款记录            ├─ 结项登记
  ├─ 合同校验比对      ├─ 应收款计算          └─ 提交结项审批
  └─ 提交立项审核      └─ 查询汇总
```

### 3.2 三阶段详细说明

#### 第一阶段：项目立项（商务）
1. 上传 Word 合同 → OCR/NLP 自动提取项目信息
2. 补充盖章 PDF 合同（支持多版本），进行合同自动校验比对
3. 提交立项审核 → 管理员审批通过后项目状态变更为「已立项」

#### 第二阶段：开票与回款（财务）
1. 选择已立项项目 → 填写开票信息并上传发票
2. 系统 OCR 识别发票信息并自动存储
3. 支持多次回款记录，自动关联开票，计算应收款

#### 第三阶段：项目结项（项目经理）
1. 选择已立项项目 → 上传验收报告（可选）
2. 记录结项时间 → 提交结项审批
3. 财务审批通过后项目状态变更为「已结项」，只读不可再开票

---

## 4. 业务规则

### 4.1 金额校验规则

| 场景 | 规则 | 处理方式 |
|------|------|----------|
| 开票金额 | 累计开票金额 **不能超过** 合同金额 | 超出时系统拦截，审批自动驳回 |
| 回款金额 | 累计回款金额 **不能超过** 开票金额 | 超出时系统拦截，不允许提交 |
| 合同金额为0 | 特殊标记为「框架协议」状态 | 不做金额校验，单独管理 |

### 4.2 项目状态流转

```
草稿 ──→ 待审核 ──→ 已立项 ──→ 进行中 ──→ 已结项
  ↑         │                    │           │
  │         ▼                    ▼           │
  └──── 驳回(回到草稿)        已终止        撤回结项(回到进行中)
```

| 状态 | 说明 | 可执行操作 |
|------|------|------------|
| 草稿 | 初始状态，可编辑 | 编辑、删除、提交审核 |
| 待审核 | 已提交，等待审批 | 审核通过/驳回 |
| 已立项 | 审核通过 | 开票、回款、结项申请 |
| 进行中 | 项目执行中 | 开票、回款、结项申请 |
| 已结项 | 项目完成，只读 | 撤回结项（仅管理员） |
| 已终止 | 项目中途取消 | 无（只读） |
| 框架协议 | 合同金额为0 | 特殊管理 |

### 4.3 驳回与撤回规则

| 场景 | 处理方式 |
|------|----------|
| 立项驳回 | 回到**草稿状态**，商务可重新编辑后再次提交 |
| 结项驳回 | 项目经理可**补充材料重新提交** |
| 结项撤回 | 已结项项目发现数据错误，管理员可**撤回结项**，状态回到「进行中」 |
| 项目终止 | 项目中途取消，状态变为「已终止」，只读不可操作 |

### 4.4 结项规则
- 结项时**不强制要求**开票回款完成
- 如有未结清款项，系统自动标记**尾款状态**：
  - 尾款金额 = 合同金额 - 累计回款金额
  - 尾款状态：待回款 / 部分回款 / 已结清

### 4.5 数据权限规则
| 角色 | 数据权限 |
|------|----------|
| 管理员 | 查看所有项目 |
| 商务 | 查看所有项目 |
| 财务 | 查看所有项目 |
| 项目经理 | 查看所有项目 |

### 4.6 项目编号规则
- 格式：`PRJ-YYYY-NNNN`
- YYYY：年份（如2024）
- NNNN：四位自增数字，每年从0001重新开始
- 示例：PRJ-2024-0001、PRJ-2024-0002、PRJ-2025-0001

---

## 5. 功能需求详述

### 4.1 系统管理模块

#### 4.1.1 用户管理
- **功能描述**：管理系统用户信息，分配角色权限
- **操作**：新增、编辑、删除、启用/禁用、重置密码
- **字段**：
  | 字段 | 类型 | 必填 | 说明 |
  |------|------|------|------|
  | 用户名 | 文本 | ✅ | 唯一标识 |
  | 姓名 | 文本 | ✅ | 真实姓名 |
  | 手机号 | 文本 | ✅ | 格式校验 |
  | 邮箱 | 文本 | 选填 | 邮箱格式 |
  | 角色 | 下拉 | ✅ | 管理员/商务/财务/项目经理 |
  | 部门 | 下拉 | 选填 | 所属部门 |
  | 状态 | 开关 | ✅ | 启用/禁用 |

#### 4.1.2 角色管理
- **功能描述**：基于 RBAC 的角色权限管理
- **操作**：新增角色、编辑角色、分配菜单权限

#### 4.1.3 数据字典
- **功能描述**：维护系统下拉选项、枚举值等基础数据
- **字典类型**：
  - 项目类型（软件开发、系统集成、咨询服务等）
  - 项目状态（草稿、待审核、已立项、进行中、已结项）
  - 合同类型（总包、分包、框架协议）
  - 发票类型（增值税专票、普票）
  - 客户行业分类
  - 回款方式（银行转账、支票、现金）

#### 4.1.4 系统日志
- **功能描述**：记录用户操作日志，支持审计追溯
- **记录内容**：操作人、操作时间、操作类型、操作内容、IP地址

---

### 4.2 工作台模块

#### 4.2.1 个人工作台
- **功能描述**：各角色登录后的首页，展示待办事项和关键数据
- **展示内容**：
  - **待办事项**：待审核立项、待审核结项、待处理任务
  - **数据概览**：
    - 项目总数
    - 进行中项目数
    - 本月开票金额
    - 本月回款金额
  - **快捷入口**：常用功能快速跳转
  - **消息通知**：系统消息、审批通知

---

### 4.3 立项管理模块

#### 4.3.1 立项前登记（商务操作）
- **功能描述**：商务上传 Word 格式合同，系统自动提取关键信息
- **操作流程**：
  1. 上传 Word 合同文件（.doc/.docx，≤20MB）
  2. 系统调用 OCR+NLP 自动识别并提取：
     - 项目名称
     - 合同金额
     - 合同编号
     - 签订时间
  3. 自动填充表单，商务可手动修正
  4. 保存草稿或直接进入下一步

- **表单字段**：
  | 字段 | 类型 | 必填 | 说明 |
  |------|------|------|------|
  | 项目名称 | 文本 | ✅ | 自动提取，可修改 |
  | 项目编号 | 文本 | 自动生成 | 格式：PRJ-YYYY-NNNN |
  | 客户名称 | 文本 | ✅ | 自动提取或手动填写 |
  | 合同金额 | 数值 | ✅ | 自动提取，单位：元 |
  | 合同编号 | 文本 | ✅ | 自动提取或手动填写 |
  | 签订日期 | 日期 | ✅ | 自动提取，可修改 |
  | 项目类型 | 下拉 | ✅ | 从数据字典选择 |
  | 项目经理 | 下拉 | ✅ | 从用户列表选择 |
  | 预计开始日期 | 日期 | ✅ | 手动填写 |
  | 预计结束日期 | 日期 | ✅ | 手动填写 |
  | 项目描述 | 多行文本 | 选填 | 项目概况说明 |
  | Word合同附件 | 文件 | ✅ | 原始上传文件 |

#### 4.3.2 立项信息登记（商务操作）
- **功能描述**：补充项目详细信息，上传盖章 PDF 合同
- **操作流程**：
  1. 补充项目其他信息（后期支出等）
  2. 上传盖章 PDF 合同（支持多版本）
  3. 记录后期支出（如有）
  4. 系统自动进行合同校验比对
  5. 输出差异清单

- **补充字段**：
  | 字段 | 类型 | 必填 | 说明 |
  |------|------|------|------|
  | 后期支出 | 数值 | 选填 | 项目后期额外支出 |
  | 支出说明 | 文本 | 选填 | 支出原因说明 |
  | PDF合同附件 | 文件 | ✅ | 盖章版PDF，支持多版本 |
  | 备注 | 文本 | 选填 | 其他说明 |

#### 4.3.3 合同校验
- **功能描述**：PDF合同OCR识别后自动与录入信息比对
- **校验流程**：
  1. PDF合同OCR识别
  2. 自动与已录入信息进行比对
  3. 生成差异清单展示
  4. 商务人工确认（通过/驳回，备注原因）

- **差异清单展示**：
  | 字段 | 登记值 | PDF识别值 | 状态 | 操作 |
  |------|--------|-----------|------|------|
  | 项目名称 | XX系统开发 | XX系统开发 | ✅ 一致 | - |
  | 合同金额 | 500,000 | 500,000 | ✅ 一致 | - |
  | 签订日期 | 2024-01-15 | 2024-01-16 | ⚠️ 不一致 | 选择 |
  | 客户名称 | XX科技 | XX科技有限公司 | ⚠️ 不一致 | 选择 |

#### 4.3.4 立项审核（管理员操作）
- **功能描述**：管理员审核立项申请，支持单个审核和批量审核
- **审核要点**：
  - 查看项目完整信息
  - 查看合同文件
  - 查看差异清单及确认结果
- **审批操作**：
  - **通过**：项目状态变为「已立项」，通知商务和项目经理
  - **驳回**：填写驳回原因，退回商务修改
- **批量审核**：
  - 支持勾选多个待审核项目
  - 批量通过：统一填写审批意见
  - 批量驳回：统一填写驳回原因
  - 批量操作日志记录

- **审批表单**：
  | 字段 | 说明 |
  |------|------|
  | 审批意见 | 必填，通过/驳回 |
  | 驳回原因 | 驳回时必填 |
  | 备注 | 选填 |

#### 4.3.5 项目列表
- **功能描述**：查看所有项目，支持多条件筛选
- **筛选条件**：项目名称、项目编号、客户名称、项目状态、项目类型、立项时间范围
- **列表字段**：项目编号、项目名称、客户、金额、状态、项目经理、立项时间、操作
- **操作**：查看详情、编辑（草稿状态）、删除（草稿状态）

---

### 4.4 开票管理模块

#### 4.4.1 开票登记（财务操作）
- **功能描述**：为已立项项目登记开票信息
- **前置条件**：项目状态为「已立项」或「进行中」
- **操作流程**：
  1. 选择已立项项目
  2. 填写开票信息
  3. 上传发票（PDF/图片）
  4. 系统OCR识别发票信息
  5. 确认并保存

- **表单字段**：
  | 字段 | 类型 | 必填 | 说明 |
  |------|------|------|------|
  | 关联项目 | 下拉 | ✅ | 选择已立项项目 |
  | 开票金额 | 数值 | ✅ | 自动识别或手动填写 |
  | 开票单位 | 文本 | ✅ | 自动识别或手动填写 |
  | 开票日期 | 日期 | ✅ | 自动识别或手动填写 |
  | 发票号码 | 文本 | ✅ | 自动识别或手动填写 |
  | 发票类型 | 下拉 | ✅ | 增值税专票/普票 |
  | 购方名称 | 文本 | ✅ | 自动识别或手动填写 |
  | 销方名称 | 文本 | ✅ | 自动识别或手动填写 |
  | 交付凭证 | 文件 | 选填 | 交付证明材料 |
  | 发票附件 | 文件 | ✅ | 发票图片或PDF |

- **OCR自动识别**：
  - 上传发票图片（jpg/png）或PDF
  - 系统自动识别发票号码、金额、日期、购买方等信息
  - 自动填充表单，财务可手动修正

- **审批触发规则**：
  - 开票金额达到合同金额的指定比例（如80%）时，可触发审批流程

#### 4.4.2 开票记录查询
- **功能描述**：查看项目开票历史
- **展示内容**：
  - 项目基本信息
  - 开票记录列表（发票号码、金额、日期、单位）
  - 开票汇总：已开票金额、剩余可开票金额

---

### 4.5 回款管理模块

#### 4.5.1 回款登记（财务操作）
- **功能描述**：记录项目回款信息，支持多次回款
- **表单字段**：
  | 字段 | 类型 | 必填 | 说明 |
  |------|------|------|------|
  | 关联项目 | 下拉 | ✅ | 选择已立项项目 |
  | 关联开票 | 下拉 | ✅ | 自动关联对应项目开票记录 |
  | 回款日期 | 日期 | ✅ | 回款到账日期 |
  | 回款金额 | 数值 | ✅ | 本次回款金额 |
  | 回款方式 | 下拉 | ✅ | 银行转账/支票/现金等 |
  | 回款凭证 | 文件 | 选填 | 银行回单等证明材料 |
  | 备注 | 文本 | 选填 | 备注说明 |

#### 4.5.2 应收款计算
- **功能描述**：系统自动计算项目应收情况
- **计算逻辑**：
  - 应收款 = 开票金额 - 累计回款金额
  - 开票进度 = 已开票金额 / 合同金额 × 100%
  - 回款进度 = 累计回款金额 / 开票金额 × 100%

---

### 4.6 查询汇总模块

#### 4.6.1 开票回款查询
- **功能描述**：按年度/月度查询项目开票回款情况
- **筛选条件**：时间范围（年度/月度）、项目名称、客户名称
- **展示内容**：
  - 项目列表及开票回款明细
  - 汇总统计数据

#### 4.6.2 数据可视化
- **功能描述**：图表可视化展示开票回款数据
- **图表类型**：
  - **柱状图**：月度开票/回款金额对比
  - **折线图**：开票回款趋势
  - **饼图**：已开票 vs 未开票金额占比

#### 4.6.3 报表导出
- **功能描述**：导出开票回款报表，支持批量导出
- **导出格式**：
  - **Excel**：明细数据导出
  - **PDF**：含图表的完整报表
- **批量导出**：
  - 支持勾选多个项目批量导出
  - 支持按筛选条件批量导出
  - 支持导出项目明细、开票记录、回款记录
- **导出内容**：
  | 导出类型 | 包含内容 |
  |----------|----------|
  | 项目明细 | 项目编号、名称、客户、金额、状态、项目经理、立项时间 |
  | 开票记录 | 发票号码、关联项目、金额、类型、日期、购方 |
  | 回款记录 | 关联项目、关联发票、金额、日期、方式、备注 |
  | 综合报表 | 项目汇总、开票汇总、回款汇总、应收款汇总 |
- **导出模板**：
  - 支持**自定义导出模板**（选择导出字段）
  - 支持**保存导出模板**供后续使用
- **异步导出**：
  - 大批量导出采用**异步任务**机制
  - 导出完成后**通知下载**
  - 支持**导出历史记录**查看

---

### 4.7 结项管理模块

#### 4.7.1 结项申请（项目经理操作）
- **功能描述**：项目经理提交项目结项申请
- **前置条件**：项目状态为「已立项」或「进行中」
- **表单字段**：
  | 字段 | 类型 | 必填 | 说明 |
  |------|------|------|------|
  | 项目名称 | 只读 | - | 自动带出 |
  | 结项时间 | 日期 | ✅ | 项目结束时间 |
  | 验收报告 | 文件 | 选填 | 上传验收报告（PDF） |
  | 结项说明 | 多行文本 | ✅ | 项目总结说明 |
  | 附件 | 文件 | 选填 | 其他支撑材料 |

#### 4.7.2 结项审批（财务操作）
- **功能描述**：财务审核结项申请，支持单个审核和批量审核
- **审核要点**：
  - 确认开票回款情况（不强制要求完成）
  - 验收报告完整性（如有）
  - 标记尾款状态
- **审批操作**：
  - **通过**：项目状态变为「已结项」，项目变为只读状态
  - **驳回**：填写驳回原因，退回项目经理补充
- **批量审核**：
  - 支持勾选多个待审核结项申请
  - 批量通过：统一填写审批意见
  - 批量驳回：统一填写驳回原因
  - 批量操作日志记录

#### 4.7.3 结项后状态
- 项目信息**可读不可编辑**
- **不可再开票**
- 所有历史记录完整保留
- **尾款状态标记**：
  - 已结清：累计回款金额 = 合同金额
  - 部分回款：累计回款金额 < 合同金额
  - 待回款：无回款记录

#### 4.7.4 结项撤回（管理员操作）
- **功能描述**：已结项项目发现数据错误，管理员可撤回结项
- **前置条件**：项目状态为「已结项」
- **操作流程**：
  1. 管理员确认需要撤回的原因
  2. 执行撤回操作
  3. 项目状态回到「进行中」
  4. 通知项目经理和财务

#### 4.7.5 结项驳回后处理
- 项目经理可**补充材料重新提交**
- 补充材料包括：验收报告、结项说明、其他支撑材料
- 重新提交后进入审批流程

---

## 5. 非功能需求

### 5.1 性能需求
| 指标 | 要求 |
|------|------|
| 页面加载时间 | ≤2秒 |
| OCR识别响应时间 | ≤5秒 |
| 并发用户数 | ≥100 |
| 数据库查询响应 | ≤1秒 |

### 5.2 安全需求
- 用户密码加密存储（BCrypt）
- 接口鉴权（JWT Token）
- 操作日志完整记录
- 文件上传类型校验和大小限制
- SQL注入防护
- RBAC权限控制

### 5.3 可用性需求
- 支持主流浏览器（Chrome、Firefox、Edge）
- 响应式设计，支持1920×1080及以上分辨率
- 友好的错误提示和操作引导
- 数据输入校验和提示

### 5.4 数据需求
- 数据备份：每日自动备份
- 数据保留：操作日志保留1年
- 数据导出：支持Excel、PDF格式

---

## 6. OCR/NLP识别服务规范

### 6.1 服务商选择

**主识别方案：PaddleOCR 本地部署**

| 优势 | 说明 |
|------|------|
| **开源免费** | 无API调用费用，长期成本低 |
| **中文识别强** | 百度出品，中文识别效果优秀 |
| **本地部署** | 数据不出内网，安全性高 |
| **模型丰富** | 支持通用文字、表格、版面分析、公式识别等 |
| **可定制** | 可针对合同/发票场景微调模型 |

**兜底方案：百度OCR API**

| 场景 | 方案 |
|------|------|
| 主识别 | PaddleOCR 本地部署 |
| 兜底方案 | 百度OCR API（PaddleOCR失败时调用） |

### 6.2 技术选型

| 模型 | 用途 | 准确率 | 速度 |
|------|------|--------|------|
| **PP-OCRv4** | 通用文字识别 | 95%+ | 快 |
| **PP-StructureV2** | 版面分析+表格识别 | 90%+ | 中等 |
| **PP-ChatOCRv3** | 智能信息提取 | 92%+ | 中等 |

**推荐组合**：
- **合同识别**：PP-OCRv4 + PP-ChatOCRv3（先OCR提取文字，再用ChatOCR提取关键信息）
- **发票识别**：PP-OCRv4 + PP-StructureV2（识别发票结构，提取关键字段）

### 6.3 部署架构

```
┌─────────────────────────────────────────────────────────┐
│                 PaddleOCR Docker 部署架构                │
└─────────────────────────────────────────────────────────┘

┌─────────────┐    ┌─────────────┐    ┌─────────────────┐
│   前端应用   │───▶│  FastAPI    │───▶│ PaddleOCR       │
│  Vue3+Element│    │   后端服务   │    │ Docker容器(GPU)  │
└─────────────┘    └─────────────┘    └─────────────────┘
                          │                   │
                          ▼                   ▼
                   ┌─────────────┐    ┌─────────────┐
                   │    MySQL    │    │  本地文件    │
                   │   Docker    │    │   存储      │
                   └─────────────┘    └─────────────┘
```

### 6.4 Docker 部署配置

#### PaddleOCR 服务 Dockerfile

```dockerfile
# Dockerfile.paddleocr
FROM paddlecloud/paddleocr:latest

# 安装依赖
RUN pip install fastapi uvicorn python-multipart

# 复制代码
COPY ./app /app

# 暴露端口
EXPOSE 8000

# 启动服务
CMD ["uvicorn", "main:app", "--host", "0.0.0.0", "--port", "8000"]
```

#### Docker Compose 配置

```yaml
# docker-compose.yaml
version: '3.8'

services:
  # 前端服务
  frontend:
    image: nginx:alpine
    ports:
      - "80:80"
    volumes:
      - ./frontend/dist:/usr/share/nginx/html
    depends_on:
      - backend

  # 后端服务
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - ./uploads:/app/uploads
    environment:
      - DATABASE_URL=mysql://user:password@db:3306/project_mgmt
      - PADDLEOCR_URL=http://paddleocr:8000
    depends_on:
      - db
      - paddleocr

  # PaddleOCR服务
  paddleocr:
    build:
      context: ./paddleocr
      dockerfile: Dockerfile.paddleocr
    ports:
      - "8001:8000"
    volumes:
      - ./uploads:/app/uploads
    environment:
      - NVIDIA_VISIBLE_DEVICES=all
    deploy:
      resources:
        reservations:
          devices:
            - capabilities: [gpu]

  # MySQL数据库
  db:
    image: mysql:8.0
    ports:
      - "3306:3306"
    environment:
      - MYSQL_ROOT_PASSWORD=root_password
      - MYSQL_DATABASE=project_mgmt
      - MYSQL_USER=user
      - MYSQL_PASSWORD=password
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data:
```

### 6.5 硬件配置建议

| 配置 | CPU方案 | GPU方案（推荐） |
|------|---------|-----------------|
| **CPU** | 8核+ | 4核+ |
| **内存** | 16GB+ | 16GB+ |
| **GPU** | 无 | NVIDIA T4/A10 |
| **存储** | 100GB+ | 100GB+ |
| **识别速度** | 2-5秒/页 | 0.5-1秒/页 |

### 6.6 准确率要求

| 文档类型 | 识别字段 | 建议准确率 | 说明 |
|----------|----------|------------|------|
| **Word合同** | 项目名称 | ≥95% | 核心字段，要求高 |
| | 合同金额 | ≥98% | 金额不能出错 |
| | 签订日期 | ≥95% | 日期格式规范 |
| | 合同编号 | ≥90% | 编号格式多样 |
| **PDF合同** | 同上 | ≥95% | 盖章后识别难度增加 |
| **发票** | 发票号码 | ≥99% | 固定格式，要求最高 |
| | 金额 | ≥99% | 金额不能出错 |
| | 日期 | ≥98% | 格式固定 |
| | 购方/销方 | ≥95% | 公司名称较长 |

**准确率标准**：
- 核心字段（金额、发票号）：≥98%
- 一般字段（名称、日期）：≥95%
- 辅助字段（地址、备注）：≥90%

### 6.3 识别失败处理策略

```
┌─────────────────────────────────────────────────────────────┐
│                     识别失败处理流程                          │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
                    ┌─────────────────┐
                    │   OCR识别请求    │
                    └────────┬────────┘
                             │
              ┌──────────────┼──────────────┐
              ▼              ▼              ▼
        ┌─────────┐    ┌─────────┐    ┌─────────┐
        │ 识别成功 │    │ 部分成功 │    │ 识别失败 │
        └────┬────┘    └────┬────┘    └────┬────┘
             │              │              │
             ▼              ▼              ▼
        自动填充       填充成功的字段    显示失败提示
             │         标记失败字段      提供手动填写
             │              │              │
             └──────────────┼──────────────┘
                            ▼
                    ┌─────────────────┐
                    │   人工确认/修正  │
                    └────────┬────────┘
                             │
                             ▼
                         保存数据
```

| 场景 | 处理方式 | 用户体验 |
|------|----------|----------|
| **完全失败** | 提示"识别失败，请手动填写" | 显示空表单，光标定位到第一个字段 |
| **部分成功** | 自动填充成功字段，失败字段标红 | 提示"以下字段识别失败，请手动补充" |
| **文件格式错误** | 提示"不支持的文件格式" | 引导用户重新上传 |
| **文件过大** | 提示"文件超过20MB限制" | 引导用户压缩后上传 |
| **网络超时** | 提示"识别超时，请重试" | 提供重试按钮 |

**核心原则**：
- **必须支持手动填写**：任何情况下都不能阻断用户操作
- **识别失败不阻断流程**：允许跳过自动识别，直接手动填写
- **提供重试机制**：网络超时时可重试

### 6.4 置信度处理

| 置信度区间 | 处理方式 | 用户提示 |
|------------|----------|----------|
| **≥95%** | 自动填充，无需确认 | 无提示 |
| **80%-95%** | 自动填充，标记为"待确认" | 提示"以下字段识别置信度较低，请确认" |
| **60%-80%** | 不自动填充，显示识别结果供选择 | 提示"请确认以下识别结果" |
| **<60%** | 不显示识别结果 | 提示"识别置信度过低，请手动填写" |

**UI展示规范**：
```
┌─────────────────────────────────────────────────────────┐
│  项目名称: [XX系统开发项目________]  ✅ 置信度: 98%      │
├─────────────────────────────────────────────────────────┤
│  合同金额: [500,000_____________]  ⚠️ 置信度: 85%      │
│            └─ 识别结果: 500000  [采用] [修改]           │
├─────────────────────────────────────────────────────────┤
│  签订日期: [____________________]  ❌ 置信度: 45%       │
│            └─ 识别失败，请手动填写                      │
└─────────────────────────────────────────────────────────┘
```

### 6.5 接口调用规范

| 参数 | 说明 | 必填 |
|------|------|------|
| image | 图片Base64编码 | 是 |
| url | 图片URL地址 | 否（与image二选一） |
| language_type | 识别语言（CHN_ENG） | 否 |
| detect_direction | 是否检测图像朝向 | 否 |

**响应格式**：
```json
{
  "words_result": [
    {
      "words": "XX系统开发项目",
      "probability": 0.98,
      "location": {"top": 100, "left": 50, "width": 200, "height": 30}
    }
  ],
  "words_result_num": 1,
  "log_id": 1234567890
}
```

---

## 7. 审批流配置规范

### 7.1 审批流架构

```
┌─────────────────────────────────────────────────────────────┐
│                      审批流配置架构                          │
└─────────────────────────────────────────────────────────────┘

┌─────────────┐    ┌─────────────┐    ┌─────────────┐
│   审批模板   │    │   审批节点   │    │   审批人    │
│  (业务类型)  │───▶│  (审批环节)  │───▶│  (审批角色) │
└─────────────┘    └─────────────┘    └─────────────┘
```

### 7.2 审批流配置表设计

#### 审批模板表 (approval_template)
| 字段 | 类型 | 说明 |
|------|------|------|
| id | int | 主键 |
| template_name | varchar | 模板名称（如"立项审批"） |
| business_type | varchar | 业务类型（project/invoice/close等） |
| status | tinyint | 启用状态（0禁用/1启用） |
| create_time | datetime | 创建时间 |

#### 审批节点表 (approval_node)
| 字段 | 类型 | 说明 |
|------|------|------|
| id | int | 主键 |
| template_id | int | 关联模板ID |
| node_name | varchar | 节点名称（如"部门审核"） |
| node_order | int | 节点顺序 |
| approval_type | varchar | 审批类型（会签/或签） |
| timeout_hours | int | 超时时间（小时） |

#### 审批人配置表 (approval_approver)
| 字段 | 类型 | 说明 |
|------|------|------|
| id | int | 主键 |
| node_id | int | 关联节点ID |
| approver_type | varchar | 审批人类型（role/user/dept） |
| approver_id | varchar | 审批人ID |

### 7.3 审批流配置方案

| 审批类型 | 配置方案 | 触发条件 |
|----------|----------|----------|
| **立项审批** | 商务提交 → 管理员审核 | 立项申请提交时 |
| **开票审批** | 财务提交 → 管理员审核 | 开票金额≥合同金额80%时触发 |
| **结项审批** | 项目经理提交 → 财务审核 → 管理员审核 | 结项申请提交时 |
| **合同变更** | 商务提交 → 管理员审核 → 财务确认 | 合同变更申请时 |

### 7.4 审批人配置方式

| 配置方式 | 说明 | 适用场景 |
|----------|------|----------|
| **按角色** | 指定角色（如"管理员"） | 通用审批 |
| **按用户** | 指定具体用户 | 特定审批 |
| **按部门** | 指定部门负责人 | 部门级审批 |
| **按上级** | 提交人的上级领导 | 组织架构审批 |

**配置示例**：
```json
{
  "立项审批": {
    "nodes": [
      {
        "name": "管理员审核",
        "order": 1,
        "approval_type": "或签",
        "timeout_hours": 48,
        "approvers": [
          {"type": "role", "id": "admin"}
        ]
      }
    ]
  },
  "结项审批": {
    "nodes": [
      {
        "name": "财务审核",
        "order": 1,
        "approval_type": "或签",
        "timeout_hours": 24,
        "approvers": [
          {"type": "role", "id": "finance"}
        ]
      },
      {
        "name": "管理员审核",
        "order": 2,
        "approval_type": "或签",
        "timeout_hours": 48,
        "approvers": [
          {"type": "role", "id": "admin"}
        ]
      }
    ]
  }
}
```

### 7.5 审批类型说明

| 类型 | 说明 | 场景 |
|------|------|------|
| **或签** | 任意一人审批通过即可 | 通用审批 |
| **会签** | 所有人都需审批通过 | 重要决策 |
| **依次审批** | 按顺序依次审批 | 多级审批 |

### 7.6 审批超时处理

| 超时时间 | 处理方式 | 通知方式 |
|----------|----------|----------|
| **24小时** | 发送提醒通知 | 站内信 |
| **48小时** | 发送催办通知 | 站内信 + 邮件 |
| **72小时** | 自动转交上级/管理员 | 站内信 + 邮件 + 短信 |

**超时配置**：
- 可配置超时时间：不同审批类型可设置不同超时时间
- 自动催办：超时后自动发送催办通知
- 自动转交：超时过长可自动转交

### 7.7 审批记录表 (approval_record)

| 字段 | 类型 | 说明 |
|------|------|------|
| id | int | 主键 |
| business_type | varchar | 业务类型 |
| business_id | int | 业务ID（如项目ID） |
| template_id | int | 审批模板ID |
| node_id | int | 当前节点ID |
| approver_id | int | 审批人ID |
| status | varchar | 状态（pending/approved/rejected） |
| opinion | varchar | 审批意见 |
| create_time | datetime | 创建时间 |
| update_time | datetime | 更新时间 |

---

## 8. 接口需求

### 8.1 外部接口
| 接口 | 说明 | 调用方 | 部署方式 |
|------|------|--------|----------|
| PaddleOCR识别接口 | 合同/发票图像识别（主） | 后端调用 | Docker本地部署 |
| 百度OCR API | 合同/发票图像识别（兜底） | 后端调用 | 云端API |
| NLP提取接口 | 文本关键信息提取 | 后端调用 | 本地部署 |

### 8.2 PaddleOCR 接口规范

**接口地址**：`http://paddleocr:8000/api/v1/ocr`

**请求方式**：POST

**请求参数**：
| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| file | File | 是 | 图片或PDF文件 |
| type | String | 否 | 识别类型：contract/invoice（默认contract） |

**响应格式**：
```json
{
  "code": 200,
  "message": "识别成功",
  "data": {
    "raw_text": [["文字内容", [坐标], 置信度], ...],
    "extracted_info": {
      "project_name": "XX系统开发项目",
      "contract_amount": "500000",
      "sign_date": "2024-01-15",
      "party_a": "XX科技有限公司",
      "contract_no": "HT-2024-001"
    },
    "confidence": {
      "project_name": 0.95,
      "contract_amount": 0.98,
      "sign_date": 0.92,
      "party_a": 0.94,
      "contract_no": 0.88
    }
  }
}
```

### 8.3 内部接口规范
- RESTful API设计
- 统一响应格式：`{code, message, data}`
- 接口版本管理：`/api/v1/`
- 认证方式：Bearer Token

---

## 9. 数据库设计概要

### 9.1 核心数据表

| 表名 | 说明 | 主要字段 |
|------|------|----------|
| sys_user | 用户表 | id, username, name, phone, role, dept, status |
| sys_role | 角色表 | id, role_name, role_code, permissions |
| sys_dict | 数据字典表 | id, dict_type, dict_code, dict_name, sort |
| project_info | 项目信息表 | id, project_no, name, customer, amount, status, pm_id, contract_no, balance_status |
| project_contract | 合同信息表 | id, project_id, version, file_type, file_path, upload_time |
| project_cost | 后期支出表 | id, project_id, amount, description, create_time |
| project_invoice | 发票信息表 | id, project_id, invoice_no, amount, date, type, buyer, seller, file |
| project_payment | 回款信息表 | id, project_id, invoice_id, amount, date, method, voucher_file |
| project_close | 结项信息表 | id, project_id, close_time, report_file, description, status |
| approval_template | 审批模板表 | id, template_name, business_type, status |
| approval_node | 审批节点表 | id, template_id, node_name, node_order, approval_type, timeout_hours |
| approval_approver | 审批人配置表 | id, node_id, approver_type, approver_id |
| approval_record | 审批记录表 | id, business_type, business_id, template_id, node_id, approver_id, status, opinion |
| ocr_recognition_log | OCR识别日志表 | id, file_path, recognition_type, raw_result, extracted_info, confidence, status, create_time |
| sys_log | 操作日志表 | id, user_id, action, content, ip, create_time |

---

## 10. 验收标准

### 10.1 功能验收
- [ ] 用户登录/退出功能正常
- [ ] 各角色权限控制生效（RBAC）
- [ ] Word合同上传和OCR识别准确率≥95%
- [ ] PDF合同校验和差异清单生成正常
- [ ] 立项审批流程完整（支持配置化）
- [ ] 发票OCR识别准确率≥98%
- [ ] 回款记录和应收款计算正确
- [ ] 结项流程完整，结项后状态正确
- [ ] 结项撤回功能正常
- [ ] 数据统计图表展示正确
- [ ] 报表导出（Excel/PDF）功能正常
- [ ] 批量导出功能正常
- [ ] 审批流配置功能正常
- [ ] 审批超时处理功能正常
- [ ] PaddleOCR Docker容器正常启动
- [ ] PaddleOCR识别失败时自动切换百度OCR兜底
- [ ] OCR识别置信度展示正常

### 10.2 性能验收
- [ ] 页面加载时间≤2秒
- [ ] PaddleOCR识别响应≤2秒（GPU）/≤5秒（CPU）
- [ ] 百度OCR兜底响应≤3秒
- [ ] 100并发用户系统稳定
- [ ] Docker容器资源占用合理（CPU<80%，内存<80%）

---

## 11. 附录

### 11.1 术语定义
| 术语 | 说明 |
|------|------|
| OCR | 光学字符识别，用于图像文字提取 |
| NLP | 自然语言处理，用于文本语义分析 |
| PaddleOCR | 百度开源OCR工具，支持多种语言识别 |
| PP-OCRv4 | PaddleOCR最新通用文字识别模型 |
| PP-StructureV2 | PaddleOCR版面分析+表格识别模型 |
| PP-ChatOCRv3 | PaddleOCR智能信息提取模型 |
| RBAC | 基于角色的访问控制 |
| Docker | 容器化部署平台 |
| 立项 | 项目正式立项，获得内部批准 |
| 结项 | 项目完成，正式关闭 |
| 会签 | 所有审批人都需审批通过 |
| 或签 | 任意一人审批通过即可 |

### 11.2 参考文档
- 企业项目管理规范
- 财务开票回款管理制度
- 合同管理办法
- AI编程培训自主选题研发.pptx
- PaddleOCR官方文档：https://github.com/PaddlePaddle/PaddleOCR
- 百度OCR API文档
