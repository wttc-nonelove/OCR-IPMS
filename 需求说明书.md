# 智能项目管理系统需求说明书（最终版）

## 1. 项目概述

### 1.1 项目背景

企业项目管理过程中，项目立项、合同审查、开票、回款、结项和报表统计往往分散在线下文档、Excel 表格和人工审批中，存在录入效率低、金额校验困难、审批记录不可追溯、项目状态不透明等问题。本系统通过 Web 化业务流程、OCR 文档识别、规则抽取和 LLM 兜底解析，建设覆盖项目全生命周期的智能项目管理系统。

### 1.2 建设目标

- 实现项目从草稿、待审核、已立项、进行中到已结项的数字化流转。
- 支持商务、管理员、财务、项目经理多角色协同。
- 支持合同、发票、回款凭证的智能识别和人工确认。
- 支持审批任务、审批记录和审计日志，保证业务过程可追踪。
- 支持开票回款台账、年度/月度报表和 Excel 导出。
- 支持 Docker Compose 一键部署。

### 1.3 技术架构

| 层级 | 技术选型 | 说明 |
|---|---|---|
| 前端 | Vue 3、TypeScript、Element Plus、Pinia、Vue Router、Vite | 浏览器端页面和交互 |
| 后端 | FastAPI、SQLAlchemy、Pydantic、JWT、BCrypt | 业务接口、权限、校验和数据访问 |
| 数据库 | MySQL 8.0 | 业务数据持久化 |
| OCR | PaddleOCR 独立服务 | PDF/图片识别，PDF 先转图片 |
| LLM | OpenAI 兼容接口 | 弱标识合同兜底解析，可配置多模型 |
| 文件存储 | 本地 uploads 目录 | 合同、发票、回款凭证、验收报告 |
| 部署 | Docker Compose | frontend、backend、db、paddleocr 四服务 |

## 2. 用户角色与权限

### 2.1 角色定义

| 角色 | 职责 | 权限说明 |
|---|---|---|
| 管理员 | 系统管理、立项审核、项目启动、跨角色任务处理 | 超级角色，可访问全部模块 |
| 商务 | 立项登记、合同解析、差异确认、提交审核 | 可维护草稿和被驳回草稿 |
| 财务 | 开票登记、回款登记、台账、结项审批、报表导出 | 可查看立项详情和财务明细 |
| 项目经理 | 查看项目、查看财务明细、提交结项 | 可上传验收报告和附件 |

### 2.2 权限规则

- 用户不允许多角色，一个用户仅绑定一个角色。
- 管理员作为超级角色，接口权限判断默认通过全部业务角色校验。
- 前端菜单只做可见性控制，后端接口权限才是安全边界。
- 所有关键操作写入 `sys_log`，便于审计追溯。

## 3. 核心业务流程

### 3.1 立项流程

1. 商务上传 Word 或 `.doc` 合同。
2. 系统解析合同文本，提取项目名称、合同金额、甲方、乙方、丙方、签订日期、合同编号等字段。
3. 合同编号允许为空；项目编号、招标编号、采购编号不作为合同编号。
4. 合同弱标识或规则缺失时，可调用 LLM 兜底解析。
5. 商务保存草稿，刷新后可恢复。
6. 商务上传盖章 PDF/图片合同，系统 OCR 后生成差异。
7. 商务确认差异，人工采用值写回项目。
8. 商务提交立项审核。
9. 管理员查看完整立项详情，填写备注后通过或驳回。
10. 审核通过后项目为已立项；管理员确认开始后项目为进行中。

### 3.2 开票回款流程

1. 财务在开票回款页搜索并选择项目。
2. 上传发票 PDF/图片，系统识别发票号码、开票日期、购方、销方、不含税金额、税率、税额、价税合计。
3. 保存发票时校验发票号码唯一，按不含税金额校验合同额度。
4. 上传回款凭证，系统优先识别“本次回款金额/本次收款金额”。
5. 回款不强制关联具体发票，按项目登记。
6. 项目累计回款金额不得超过项目累计开票价税合计。
7. 台账展示合同金额、累计开票、累计回款、未回款金额、进度和明细。

### 3.3 结项流程

1. 项目经理选择已立项或进行中的项目。
2. 填写结项时间、结项说明，上传验收报告或附件。
3. 如项目回款未完成，系统提示未回款金额，但不强制阻断。
4. 财务查看结项内容、项目基础信息和财务明细。
5. 财务审批通过后项目状态变为已结项。
6. 已结项项目只读，不允许继续开票。

### 3.4 查询报表与导出

- 支持年度查询，展示 1-12 月开票和回款趋势。
- 支持月度查询，展示当月每日趋势和当月明细。
- 支持项目编号、合同编号、项目名称、甲方/客户关键字。
- 导出任务不落库，只存在运行时队列。
- 导出完成后通过浏览器下载 `.xlsx`。

## 4. 业务规则

### 4.1 项目编号规则

- 格式：`PRJ-YYYY-NNNN`。
- `YYYY` 为年份。
- `NNNN` 为四位自增序号，每年从 `0001` 开始。

### 4.2 项目状态流转

```text
草稿 -> 待审核 -> 已立项 -> 进行中 -> 结项审批中 -> 已结项
  ^        |
  |        v
  +---- 已驳回草稿
```

### 4.3 合同解析规则

- `.doc/.docx`：文档解析 + 规则抽取 + 可选 LLM 兜底。
- PDF/图片：PaddleOCR + 规则抽取 + 可选 LLM 兜底。
- 合同金额优先取“本合同总金额、合同总金额、合同金额、合同价款”等总价字段。
- 规则明确识别出的总金额、甲乙方和日期优先于 LLM。
- 识别失败不阻断草稿保存，允许人工补充。

### 4.4 财务金额规则

| 场景 | 规则 |
|---|---|
| 发票号码 | 全局唯一 |
| 开票额度 | 累计不含税金额不得超过合同金额 |
| 价税合计 | 发票 `amount` 保存价税合计 |
| 回款登记 | 不强制关联单张发票 |
| 回款上限 | 项目累计回款不得超过项目累计开票价税合计 |
| 已结项项目 | 只读，不可继续开票 |

### 4.5 删除规则

- 不实现软删除。
- 商务只能删除自己创建的草稿。
- 管理员可按业务需要删除测试项目。
- 发票删除后若导致项目累计回款超过剩余累计开票价税合计，则拒绝删除。
- 删除操作写入审计日志。

## 5. 功能需求

### 5.1 系统管理

- 用户新增、编辑、启用、禁用、删除。
- 用户筛选和用户统计。
- 数据字典查看。
- 审批流模板查看。
- 审计日志和 OCR 日志查看，日志区域内部滚动。
- PaddleOCR 健康状态查看。
- LLM 多模型配置、启停开关、API Key 掩码、连接测试。

### 5.2 工作台

- 展示角色相关指标。
- 展示待办任务。
- 展示项目生命周期统计。
- 展示开票回款趋势。

### 5.3 立项管理

- Word/.doc 合同上传解析。
- 草稿保存和刷新恢复。
- 合同重新上传。
- 盖章 PDF/图片合同 OCR。
- 合同差异生成和确认。
- 提交审核、管理员审批、项目开始确认。
- 项目列表支持关键字和年份筛选。

### 5.4 开票回款

- 页面内 Tabs：开票登记、回款登记、发票记录、回款记录、台账。
- 项目选择支持输入关键字实时联想。
- 发票上传识别和保存。
- 回款凭证上传识别和保存。
- 发票、回款删除重登。
- 台账实时刷新。

### 5.5 结项管理

- 项目经理提交结项申请。
- 上传验收报告和附件。
- 回款未完成提示。
- 财务查看结项详情并审批。
- 已结项项目只读。

### 5.6 查询报表

- 年度/月度查询。
- 趋势图展示。
- 发票和回款明细展示。
- Excel 导出和下载。

## 6. 非功能需求

- 文件上传限制类型和大小。
- OCR 失败时记录日志并允许人工录入。
- 金额字段使用 `DECIMAL`。
- 数据库/磁盘层采用透明加密能力，不做字段级金额加密。
- 系统支持 Docker Compose 一键启动。
- 接口统一使用 `/api/v1/` 前缀和 `{code, message, data}` 响应格式。
